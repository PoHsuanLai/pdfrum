# M15 — JavaScript via `boa` — status

Running record. The brief is `docs/design/pdfrum-script.md` (614ab62); the
rulings on its escalations are in PLAN.md §M15, SPEC.md §10 and STYLE.md §2b
(66e6014, and the E2 settlement below).

## E2 settled: the sandbox, measured against the oracle rather than read

The brief measured that boa's `RuntimeLimits` bound loops, recursion and
stack but not heap growth or regex backtracking. The user's ruling was to
document that *provided PDFium with V8 bounds none of it either*, and to
measure rather than assert. Our oracle build has `pdf_enable_v8=false`, so
the measurement used bblanchon/pdfium-binaries `chromium/8021`
(154.0.8021.0, 2026-08-25 — three days from our pinned `6f2272e`), driven by
a ~100-line C program over the public API that mirrors `pdfium_test`'s alert
hook. Driver, scripts and the full report: `docs/status/data/v8probe/`.

| case | PDFium + V8 | boa |
|---|---|---|
| `while(true){}` | never returns; SIGKILLed externally at 120 s and 600 s, 2 MB RSS | `RuntimeLimitError` in ~10 ms |
| heap growth (`Array(1000)` ×2M) | 860 MB cleanly at ×200k; at ×2M **1.58 GB then V8's `Reached heap limit` — SIGTRAP, core dump**, no PDFium callback | unbounded |
| `s=s+s` ×30 | stops at 17 MB on V8's intrinsic 2²⁹ `String::kMaxLength` as a RangeError PDFium **swallows silently** (no alert) | 1 GiB in 1.1 s |
| `/(a+)+$/` len 20/24/26/28/32/34 | 0.02 / 0.18 / 0.80 / 3.13 / 56.98 s / **killed at 120 s**; ~4× per +2 | same algorithm, same growth |

API audit of the shipped headers: no timeout, budget or terminate knob;
`FPDF_GetRecommendedV8Flags()` is `"--jitless"` alone; the only authored cap
is `kMaxAllowedBytes = 0x10000000` per `ArrayBuffer`, irrelevant to all three.
`ulimit -v` cannot even be used as an external control — V8's cage
reservation aborts at *initialisation* under it; a cgroup RSS cap is the
working external mechanism, for PDFium and for us alike.

**So the sentence PLAN §M15 wanted — "a stronger property than the C++ has"
— is true as written for loops, recursion and stack, and the two holes are
holes the oracle has too, one of them worse (a crash, not a hang).** Recorded
in SPEC §10 with the reopening condition.

## `AF*`: where we diverge from the oracle on purpose

The `AF*` library landed as `crates/pdfrum-script` (see the review at
`docs/reviews/claude-review-af-library.md` for the crate-versus-module
reconciliation). While reviewing it against `fxjs/cjs_publicmethods.cpp`,
`fxjs/cjs_util.cpp` and `fxjs/fx_date_helpers.cpp`, six behaviours turned out
to be **defects in the oracle rather than Acrobat's specification**. Each was
checked at its cited line, and against pdf.js — Mozilla's independent
implementation of the same Adobe-specified library — as an impartial second
reading.

**We implement the correct behaviour and diverge from the goldens where they
pin the defect.** Reproducing a one-character typo because a fixture records
its output would make the fixture, not the specification, our contract. Each
divergence is marked `// [oracle-bug]` in the source with both citations.

### The six

| # | What the oracle does | Where | What is correct | Second reading |
|---|---|---|---|---|
| 1 | 2000 is a common year and 1900 is a leap year | `fx_date_helpers.cpp:71` | The Gregorian rule: 2000 leap, 1900 common | pdf.js defers to JS `Date` |
| 2 | Every two-digit year becomes 2000–2099, so `85` is 2085 | `fx_date_helpers.cpp:330,556` | Pivot at fifty: `85` is 1985, `05` is 2005 | pdf.js `util.js:372-376` |
| 3 | Noon is `am`, and midnight prints as hour `0` | `cjs_publicmethods.cpp:517-535` | Noon is `pm`; both noon and midnight print as `12` | pdf.js `util.js:240-247` |
| 4 | `AFSimple('avg', …)` adds without dividing; `'min'` does not seed | `cjs_publicmethods.cpp:215-232` vs `:1341,1442,1456` | One name, one answer, whatever its case | pdf.js `aform.js:350-363` |
| 5 | `util.printd`'s `dddd`/`ddd` print `Sunday`/`Sun` for every date | `cjs_util.cpp:267` | The real weekday | pdf.js reads `oDate.getDay()` |
| 6 | `IsNumber("")` and `IsNumber("  ")` are `true` | `cjs_publicmethods.cpp:272-309` | Nothing is not a number | the oracle's own `TODO(weili)` |

Each in one line:

1. **Leap year.** `IsLeapYear` reads
   `(year % 4 == 0) && ((year % 100 != 0) || (year % 400 != 0))`. The last
   clause should be `year % 400 == 0`; as written it is true for every year not
   divisible by 400, so the century exception never fires and its own exception
   fires backwards. The only leap year `fx_date_helpers_unittest.cpp` checks is
   1972, which both spellings agree on. It also puts the test at odds with
   `DayFromYear` two functions above it, which counts leap days correctly.
2. **Two-digit years.** `int nYearSub = 99;  // nYear - 2000;` — the intended
   formula is in the comment and was never written; `:556` then maps `0..=99`
   to `2000..=2099` unconditionally. A birth date typed `31/12/85` lands in
   2085.
3. **The twelve-hour clock.** `nHour > 12 ? nHour - 12 : nHour` leaves midnight
   as `0`, and the matching `nHour > 12 ? 'p' : 'a'` labels noon `am`.
4. **`AVG` and the case of the name.** `ApplyNamedOperation` matches with
   `EqualsASCIINoCase`, but the average's divide and the extremum's seed test
   the name again with the case-*sensitive* `EqualsASCII`. So `'avg'` reaches
   the combining step, adds, and never divides — answering a sum under an
   average's name.
5. **The weekday.** `struct tm time = {}` is filled with year, month, day, hour,
   minute and second but never `tm_wday`, which `wcsftime`'s `%A`/`%a` read
   directly rather than deriving.
6. **The empty string.** The walk returns true on reaching the terminator. The
   oracle's own unit test carries
   `// TODO(weili): Check whether results from case 0, 1, 10, 15 are intended.`,
   naming these two among them.

### What this costs against the goldens: two assertions

Measured, not estimated. Every assertion the `AF*` fixtures carry that a pure
function can answer was driven against the corrected implementation:

| fixture | assertions | reachable without an engine | byte-exact | oracle-bug bucket |
|---|---|---|---|---|
| `public_methods_expected.txt` | 356 | 291 | 291 | 0 |
| `util_printx_expected.txt` | 37 | 35 | 35 | 0 |
| `util_printd_expected.txt` | 57 | 54 | 52 | **2** |
| `util_scand_expected.txt` | 9 | 7 | 7 | 0 |
| **total** | **459** | **387** | **385** | **2** |

The unreachable remainder is argument-count checking, which no function in this
crate can see — arity belongs to the engine binding, and every one of those
assertions expects the same `Incorrect number of parameters passed to
function.` that `Error::ParamCount` carries.

The two in the bucket, in full:

| line | fixture asserts | we answer | why |
|---|---|---|---|
| `util_printd_expected.txt:13` | `Alert: dddd: Sunday` | `Friday` | #5 — the date is 4 July 2014, a Friday |
| `util_printd_expected.txt:14` | `Alert: ddd: Sun` | `Fri` | #5, same date |

This is the same bucket the three V8-string goldens already occupy under the
M15 exit criterion (`apply`, `array_buffer`, `immutable_proto`, PLAN §M15,
66e6014): accounted for, not passing, and not passing *on purpose*.

Divergences 1, 2, 3, 4 and 6 cost **nothing** against the goldens — none of the
fixtures exercises a century year, a two-digit year, noon or midnight, a
lowercased operation name, or an empty commit. They are corrected because a
document in the wild will exercise them even though no fixture does. The tests
that pin them live in `crates/pdfrum-script`, asserting the correct answer with
its pdf.js citation.

**The M15 exit count therefore becomes 47/47 accounted for — 44 byte-exact, 3
not achievable by construction (V8 strings) — plus, for the `AF*` fixtures,
385/387 byte-exact and 2 not achievable by construction (oracle bug #5).**

### Reportable upstream

Divergences 1 and 2 are one-line repros and worth filing at crbug.com/pdfium;
the paragraphs are in the review's closing section.

*2026-09-02:* filed-ready text is `docs/upstream/pdfium-js-issues.md` — five
issues in the tracker's template with observed output from PDFium+V8
(`chromium/8021`) and repro PDFs in `docs/upstream/pdfium-repro/`. The
review's `29/02/2000 → kBadDate` repro was wrong and is annotated there.

> *Later the same day:* all five were **filed** (crbug.com/pdfium/555821585,
> /555821586, /555848528, /555848529, /555848530), and `docs/upstream/` was
> reorganised to one file per issue — the paths above are historical; see
> `docs/upstream/README.md` for the index.

---

## Step 1 — the cascade threaded, and the engine bound

Landed 2026-09-02 in three commits, each path-scoped:

| commit | what |
|---|---|
| `05f3138` | the cascade threaded through routing, `/CO`, the four `Limits` fields, A63 |
| `ac89ebd` | `[spec]` — SPEC §15.1, §15.7 and §10 for the above |
| `af363b5` | `boa` admitted and bound: the engine, `util`, `app.alert`, the transcript, the sandbox tests |

### What M14's exit criteria could not have caught

The brief's E1 was right, and it is worth restating as the finding rather
than as a premise. M14 shipped `Cascade` and `NoScripts`, every one of its
tests passed, and **none of the seam's three wires existed**:

- `commit::run` was called by nothing but its own unit tests;
- `route::Context` had no cascade and `route::apply` no parameter for one;
- `Cascade::keystroke` had no call site anywhere in the crate.

No exit criterion could have caught this, because `NoScripts` *is* the
identity cascade: a cascade that changes nothing is indistinguishable from
one that never runs. The tests that would have caught it are the eight in
`crates/pdfrum-form/tests/cascade_wiring.rs`, and they assert the opposite
property from everything else in the suite — that a cascade which **does**
something changes what routing does. Each fails if the hook it names loses
its caller again.

### E1: a parameter, not a `Context` field

Ruled, and landed as ruled. `apply`, `choose` and `kill_focus` take
`cascade: &mut dyn Cascade` second; `close_popup`, `focus_of`, `popup_view`
and `scroll_view` commit nothing and are unchanged. Internally the parameter
reaches fifteen functions, computed as the transitive closure over the call
graph rather than guessed.

`choose` takes the parameter and does not yet spend it, and that is upstream's
shape rather than an omission: `CFFL_ComboBox::SaveData`
(`fpdfsdk/formfiller/cffl_combobox.cpp:90`) runs only from `CommitData`,
which only `KillFocusForAnnot` calls, so choosing a row changes the selection
and the scripts run when the field is left. The parameter is on the signature
because E1 names it one of the three entry points that *can* reach a commit,
and a caller must not have to discover later that it needs one.

**The board did not move.** `conformance run --check-regressions` reports no
regressions, and a field-by-field diff of all 1705 rows against the committed
board — status, tags, `tier_a`, `tier_b` — reports **0 changed, 0 vanished, 0
appeared**. That is the whole of what "byte-identical behaviour for every
existing caller" means, and it is measured rather than asserted.

### E6: `/CO`, and the rule that decides it

`Form::calculation_order(&self, catalog, r) -> Vec<usize>` in `pdfrum-doc`,
additive. The rule that matters is the negative one: **a document with no
`/CO` array runs no calculation at all**, however many of its fields carry
`/AA /C`. `CountFieldsInCalculationOrder` returns 0 and
`GetFieldInCalculationOrder` returns null the moment `GetArrayFor("CO")`
finds nothing (`core/fpdfdoc/cpdf_interactiveform.cpp:739-761`), and the
sweep walks exactly that list — so an empty answer is *the* answer, and a
reader tempted to fall back to "every field, in `/Fields` order" would
recalculate documents the oracle leaves alone, visibly, on any file with a
calculation script and no `/CO`.

### E4: the four `Limits` fields, and one correction

Landed as ruled. One thing the brief did not know: `SessionConfig` already
carried a `max_calculate_depth`, added during M14 and defaulting to **8**.
Upstream's `busy_` (`fpdfsdk/cpdfsdk_interactiveform.cpp:259-264`) is a flag,
not a counter — the outer sweep is authoritative and every nested call
returns immediately — so the oracle permits **no** nesting at all. The field
now reads `Limits::default().max_calculate_depth`, which is 1.

### A63 — a failing validate keeps focus `[oracle-bug]`

Assigned here because it sits in the path this step rewired. PDFium's
`CFFL_FormField::CommitData` returns `true` from both refusal paths
(`fpdfsdk/formfiller/cffl_formfield.cpp:525-531`), and that return value is
`KillFocusForAnnot`'s only guard (`:306`), so `pWnd->KillFocus()` (`:311`)
and `EscapeFiller()` (`:324`) run whether the value was accepted or refused.
A user who typed something a validation script rejected finds the caret gone
and their typing discarded, with no way to correct it — which defeats the
purpose of a validation hook.

ISO 32000-1 §12.7.5.3 gives Validate the job of rejecting the *value*, not of
ending the interaction, and pdf.js implements that rule with the comment to
prove it: on the not-valid branch it sends `focus: true, // Stay in the
field.` (`src/scripting_api/event.js:277`, verified at the line). Under
PLAN.md's oracle-bug rule pdfrum reverts the edit **and keeps the field**;
`CommitOutcome::keeps_focus` is that answer, and `commit.rs`'s module
documentation carries both citations. **Zero board cost**, because the branch
is unreachable without scripts — nothing can refuse.

The `commit.rs` module doc that described the old behaviour as "the quirk
that must not be tidied" is replaced rather than amended: it was a correct
description of the oracle and a wrong instruction about what to do with it.

### `Keystroke`'s indices become `i32`, and `applied()` stops clamping

Brief §2.3 A4, and the reason is sharper than "the type was too narrow".
`CalcMergedString` (`fxjs/cjs_publicmethods.cpp:127-137`) has two halves that
**do not agree with each other**:

- the prefix is `value.First(SelStart())`, and `First(n)` is `Substr(0, n)`,
  which returns the **empty** string whenever `n` exceeds the length
  (`core/fxcrt/string_view_template.h:226-249`). `SelStart()` is an `int`
  widened to `size_t`, so `event.selStart = -1` produces an enormous count
  and an empty prefix — the field's leading text vanishes.
- the suffix is guarded explicitly by `end >= 0 && end < length`, which for a
  suffix is the same answer a clamp would give.

The old `.min(len)` clamp was the reasonable behaviour and the wrong one; it
kept text the oracle drops, on an input any `/AA /K` script produces in one
assignment. A reversed selection is also not swapped: prefix and suffix are
read independently, so crossing them duplicates text.

### Where `ScriptCascade` lives, and the brief's premise that changed

The brief §2.4 recommended a feature-gated module in `pdfrum-form` rather than
a new crate, and argued it from the dependency graph — every input except
`boa_engine` was already in `pdfrum-form`'s tree, `AF*` included, because
§1.2 put `AF*` in `crates/pdfrum-form/src/af/`.

**`AF*` shipped as `crates/pdfrum-script`, a separate crate.** So the argument
was re-run against what exists:

| where `ScriptCascade` lives | edges it needs | verdict |
|---|---|---|
| `pdfrum-script` | `pdfrum-script → pdfrum-form` (for `Cascade`), and then `pdfrum-form → pdfrum-script` to construct one | **the back-edge §2.4 warned about**, or a facade that makes every consumer know both names |
| `pdfrum-form::script` | `pdfrum-form → pdfrum-script`, one forward edge, plus `boa_engine` | clean |

The recommendation survives its premise. The counter-argument §2.4 stated
fairly also survives: a cargo feature is the **weaker** isolation mechanism,
because feature unification means any workspace member can turn it on for the
whole build, where the GPU backend is isolated by being a crate nothing
depends on. That is exactly why `scripts/check-no-boa.sh` is not optional and
why it carries the fourth, converse assertion.

### The binding shape, which is what the follow-on copies

Stated once in `bind.rs`'s module documentation so a follow-on adding object
model does not have to invent it:

1. **A free `fn(&JsValue, &[JsValue], &mut Context) -> JsResult<JsValue>`**,
   registered with `NativeFunction::from_fn_ptr`. Not a closure — boa's
   closures must be `Copy`, which rules out capturing host state.
2. **The host state lives in boa's own `HostDefined` slot**, so a function
   pointer reaches the transcript through the `&mut Context` it already has.
   `Context::get_data` hands back `&T`, hence `Rc<RefCell<HostState>>`.
3. **A missing argument is `undefined`, not an error** — several fixtures
   assert the resulting message rather than a panic.

A stub is never a `todo!()` and never a panic. Most of the Tier-2 surface is
"returns success and does nothing" *upstream*, so reproducing that is correct
behaviour rather than a shortcut, and a script calling `app.browseForDoc`
must not bring the process down.

### The sandbox, tested rather than claimed

The E2 measurement above said what the bounds are; these are the tests that
hold us to them. All in `crates/pdfrum-form/src/script/tests.rs`, and the
whole file runs in **0.159 s**.

| property | how it is asserted |
|---|---|
| `while(true)` terminates | the hook **refuses** and a `ScriptLimitReached` diagnostic is recorded — 0.115 s, against PDFium+V8's external SIGKILL at 600 s |
| deep recursion terminates | same shape, `max_script_recursion = 64` |
| stack exhaustion terminates | same shape, `max_script_stack = 512` |
| every hook refuses rather than accepts | four hooks, four refusals, one test |
| `max_calculate_depth = 1` refuses nesting | the second, nested sweep writes nothing |
| no I/O is reachable | 27 names enumerated in-engine and asserted `undefined` |
| a hostile field value cannot escape its literal | six spellings, including ` ` and a quote-comment splice |

**The assertions are on the diagnostic and the refusal, never on elapsed
time.** A timing assertion would be flaky on a loaded machine — this project
has measured its own at load 42 — and it would also be the wrong claim: what
matters is not that the script was fast, it is that it *stopped* and that the
hook took its **refusing** answer. Taking the permissive one would be the
failure mode that lets a hostile file walk past a validator, so that is the
half the tests actually check.

The I/O test is an enumeration rather than a reading of the source, which is
the difference between a security claim and a security property.
`SharedArrayBuffer` is deliberately **not** on the list: boa provides it, it
reaches nothing outside the realm, and no fixture asks about it.
`WebAssembly` and `gc` are, because `v8_features_expected.txt` asserts
`typeof` each and boa provides neither — which is a golden that passes for
free.

### boa's admission, measured

Full audit in DEPS.md. The headline numbers: **116 crates added, zero
`-sys`, zero `cc`/`cmake`/`bindgen`, `cargo deny --all-features check` clean
with no `deny.toml` edit needed** (every `OR` licence expression resolves to
an allowlisted branch), zero boa crates in any workspace member's default
tree, MSRV 1.91.0.

`default-features = false` is load-bearing rather than tidiness: the three
defaults are `float16`, `xsum` and `temporal`, and `temporal` alone drags
`icu_calendar`, `temporal_rs` and `timezone_provider` — an ICU tree, in a
project whose dependency manifest boasts that `unicode-bidi` "replaces the
entire ICU dependency", for a `Temporal` API no PDF script has ever called.
The dates PDF scripts *do* use go through `util.printd`/`util.scand`, which
are pure functions in `pdfrum-script` and reach no engine at all.

**M13 inherits one fact**: the MSRV floor is per-*feature*, not per-workspace,
because the feature is default-off. It must be written down in M13's
declaration rather than discovered by a consumer.

### A fourth and fifth not-achievable-by-construction fixture, found by running

The brief §5.3 records `gc` and `WebAssembly` as "verified absent: `typeof gc`
and `typeof WebAssembly` are both `"undefined"` in boa, **which is what
`v8_features_expected.txt` asserts**. Free."

**It asserts the opposite for `gc`**, and the golden says so in two lines:

```
Alert: PASS: typeof gc = function
Alert: PASS: typeof WebAssembly = undefined
```

Tracing where `gc` comes from settles what to do about it. It is **not**
PDFium's: `FPDF_GetRecommendedV8Flags()` returns `"--jitless"` alone
(`fpdfsdk/fpdf_view.cpp:1313-1318`), and the only `--expose-gc` in the tree is
`testing/v8_initializer.cpp:89` — `"--predictable --expose-gc"`, a **test
harness** flag. A production PDFium embedding has no `gc` either. So the
assertion is about the fixture runner's own V8 configuration, not about PDF,
which is exactly the category E3 put `apply`, `array_buffer` and
`immutable_proto` in.

Two fixtures depend on it:

| fixture | what it needs | bucket |
|---|---|---|
| `v8_features` | `typeof gc === "function"` | V8-intrinsic |
| `foreground_task` | `gc({type:'major', execution:'async'})` returning a thenable, and its `.then` resolving after an `app.alert` | V8-intrinsic |

**We decline both rather than bind a fake `gc`.** A stub would make
`v8_features` pass by asserting something false about ourselves — the whole
point of that fixture is to report which engine intrinsics the runtime has —
and `foreground_task`'s is worse, because it wants a *specific asynchronous
scheduling order* out of a garbage collector we do not have and would be
faking. Under the same reasoning E3 used to decline V8's error strings —
"that would be asserting something about a language, not about PDF" — this is
asserting something about a debug build's flags.

**So the exit restates once more: 47/47 accounted for, 42 byte-exact, 5 not
achievable by construction** — `apply`, `array_buffer` and `immutable_proto`
(V8's own exception text) plus `v8_features` and `foreground_task` (V8's
`--expose-gc` intrinsic, which is the test harness's and not PDFium's). Five
declines, each with its citation, none of them a shortcut.

### The defect a scoring run would have found, found by reading instead

**Every error a bound function throws carries its own name**, and the binding
threw bare messages. `JSFormatErrorString` (`fxjs/js_resources.cpp:97-108`)
is `class_name` + optional `"." + property_name` + `": "` + the message, and
every throw in `fxjs/` routes through it — the `AF*` wrapper at
`cjs_publicmethods.cpp:159-161` and `JSMethod`/`JSPropGetter` alike. So the
goldens read

```
Alert: PASS: AFDate_Format() threw AFDate_Format: Incorrect number of parameters passed to function.
Alert: PASS: util.printd: Incorrect number of parameters passed to function.
```

Roughly **seventy** of the golden assertions are arity checks, across
`public_methods`, the four `util_*` fixtures, `app_methods` and
`field_methods`, and every one quotes the qualified form. A bare message
fails all of them — which makes this a systematic scoring defect rather than
a nicety, and it is the single largest thing the first scoring run would have
reported.

It is recorded here rather than quietly fixed because of *how* it was found:
by reading `public_methods_expected.txt` line by line before running
anything. The brief's §1's own accounting had already noticed the shape —
"the unreachable remainder is argument-count checking… arity belongs to the
engine binding" — and named the message; what it did not say is that the
message is *prefixed*. The name is now threaded once per function (the `af!`
macro takes it as a literal), and
`every_thrown_error_carries_the_function_name` pins all four shapes.

### The 11 V8-gated formfill tests, after step 1

M14 recorded these outside its denominator (M14.md § "Bucket 2"); the M15 exit
asks for 11/11 **accounted for**, in M14's own idiom. Where step 1 leaves
them:

| test | what it asserts | after step 1 |
|---|---|---|
| `DisableJavaScript` | with scripting off, timers and intervals fire nothing over 7 s of simulated time | **passes by construction, twice over.** With the `script` feature off there is no engine to fire anything, and with it on `setTimeOut`/`setInterval` are recorded and never fired (brief §3.5's ruling — no global registry, ever). `scripts/check-no-boa.sh` is the mechanical half |
| `DocumentAActionsDisableJavaScript` | the same for `/AA` document actions | as above |
| `DocumentAActions` | `FORM_DoDocumentAAction` for WS/DS/WP/DP alerts `Will Save` / `Did Save` / `Will Print` / `Did Print`, in order | **owed**: the engine and the alert transcript exist, the *document*-level `/AA` walk does not. It is the follow-on's, and it is small — `pdfrum-doc`'s `nav::additional_action` already reads the dictionary |
| `Bug551248` | a timer fires an alert after `AdvanceTime` | **owed to a later milestone**, not to the follow-on: firing timers needs `advance_time`, which M14's D14 reserved as the step function and which nothing calls yet |
| `Bug620428`, `Bug634394`, `Bug634716`, `Bug679649`, `Bug707673`, `Bug765384`, `Bug1477093` | seven crash-and-alert regressions over `Doc`/`Field` mutation | **owed to the follow-on**, and they are the reason its object-model slice is worth doing as one piece: each needs `this.getField` plus one or two `Field` properties, and none needs anything step 1 declined |

**2 of 11 pass, 9 owed, none reclassified.** Nothing was moved into a
not-portable bucket to make a number come out — every one of the nine is
ordinary work with a named owner.

### Two more defects the goldens found, both in the clock

**The timezone `TZ=America/Los_Angeles` names is not the one `util.printd`
uses**, and getting that wrong is an hour on every date line the fixture
prints. July in that zone is PDT (−0700), which is what boa's `Date` applies
and what V8 applies — and the golden prints an hour earlier still. The reason
is two lines in the test binary:

```
FSDK_SetTimeFunction([]() { return time_ret; });
FSDK_SetLocaltimeFunction([](const time_t* tp) { return gmtime(tp); });
```
(`testing/pdfium_test/pdfium_test.cc:2133-2134`)

`localtime` is replaced by **`gmtime`**. So `GetDaylightSavingTA` reads
`tm_isdst == 0` for every instant and contributes nothing
(`fxjs/fx_date_helpers.cpp:54-68`), while `GetLocalTZA` still reads glibc's
`timezone` global — the zone's *standard* offset, PST, −8 hours (`:38-52`).
`FX_LocalTime` is their sum (`:254-256`). V8's own `Date` never goes through
those hooks and keeps the real −7.

**So the two offsets are an hour apart all summer, by construction**, and
`ScriptConfig` carries both rather than one: `timezone_offset_secs` for the
engine's `Date`, `printd_offset_secs` for `FX_LocalTime`. A reader who
"fixes" them to agree breaks `util_printd`, and a reader who sets both to −8
breaks `util_scand` — whose every line round-trips to the UTC string it was
given, which only holds at −7. Both directions were tried and measured.

**And `util.printd` type-checks rather than coerces.** Four gates, each with
its own message, all four asserted (`fxjs/cjs_util.cpp:172-222`): the second
argument must be a `Date` *object* (`42` and `"clams"` are refused, not
converted); it must not hold NaN, which `new Date(undefined)` does; a numeric
format outside `0..=2` is a *value* error; and a format that is neither
number nor string is a *type* error — which is why `printd({clams: 3}, d)`
does not stringify its argument, and is the kind of thing a coercing
implementation gets wrong silently.

Together these took `util_printd` from 60 differing lines to 12, of which two
are the known `[oracle-bug]` weekday lines already in the accounting and the
rest are overflow-constructed dates (`new Date(2014, 20, 04, …)`) where boa
and V8 resolve the overflow differently. Named rather than papered over.

### The follow-on slice, carved to be mechanical against the shipped binding

Step 1 built the engine, the transcript, `util`, `app.alert` and the `AF*`
globals. What is left is **object-model boilerplate**, and it is boilerplate
in the precise sense: every item below is the same three-part shape
`bind.rs`'s module documentation states, applied to a different name, against
a C++ file that is a table. Nothing in it requires a decision that step 1 did
not already make.

**Counted from the oracle, not estimated:**

| slice | what | oracle | size |
|---|---|---|---|
| **1. `Doc` as the global** | 22 of 47 fixtures reach `this.*`. `Document` **is** the V8 global, so `this.getField` and bare `getField` are one call. `getField`, `getNthFieldName`, `numFields`, `numPages`, `calculateNow`, `resetForm`, `getAnnot(s)`, `gotoNamedDest`, plus the ~32 metadata properties | `fxjs/cjs_document.cpp`, 1538 lines | 32 properties + 39 methods, of which **most are the Tier-2 "no-op returning success"** the inventory lists — one shared body, not 39 |
| **2. `Field`** | 19 fixtures. `value`/`valueAsString` is the load-bearing pair; the rest is the property walk `field_properties.in` does | `fxjs/cjs_field.cpp`, 2897 lines | **51 properties + 26 methods**, and ~30 of the setters **validate then do nothing** — the validation errors *are* the assertions, so this is the densest and most mechanical item on the list |
| **3. the rest of `app`** | the properties beyond the eight step 1 bound | `cjs_app.cpp`, 611 lines | small |
| **4. `color`** | 12 named constants + `convert` + `equal`, over the `["T"]/["G",g]/["RGB",…]/["CMYK",…]` array encoding | `cjs_color.cpp` | ~14 names, one encoding |
| **5. `global`** | one fixture, but it needs **interceptor semantics**: deletion is a *tombstone* (`bDeleted = true`, `:199`), assigning `undefined` deletes (`:231-263`), and `setPersistent` on a tombstone fails with `Global value not found.` **Persistence is never implemented** — `CJS_Global`'s constructor passes a hard-coded `nullptr` delegate (`cjs_global.cpp:178-181`, `GetRetainedInstance(nullptr)`), and `LoadGlobalPersistentVariables` returns `false` on its first line when the delegate is null (`fxjs/cfx_globaldata.cpp:263-266`), as does the save. Verified at both lines. So the RC4-encrypted store is dead code upstream, `setPersistent` sets a flag nothing reads, and reproducing the flag without the file is *exact* — do not implement the store; it would be the library's only file-writing path | `cjs_global.cpp`, 560 lines | the only item here with a real trap; read §3.4 before starting |
| **6. the nine constant namespaces + `IDS_*`/`RE_*`** | `border`, `display`, `font`, `highlight`, `position`, `scaleHow`, `scaleWhen`, `style`, `zoomtype`, plus 23 bare globals | `cjs_globalconsts.cpp` (48 lines), `cjs_globalarrays.cpp` (90) | ~60 constants; pure tables |
| **7. `Annot`, `Icon`** | three properties and one read-only `name` | `cjs_annot.cpp` (116), `cjs_icon.cpp` (38) | trivial |

**What makes it safe to hand over.** The binding shape is fixed and stated;
the transcript is a value type with its formats already transcribed from the
`printf` calls that produce them; the error messages are `pdfrum-script`'s
`Error` and are already API; and the hard rulings — no fourth seam, no global
state, no file written, a stub is never a panic, a decline carries the
oracle's own message rather than a new one — are all *already made*, with the
tests that hold them.

**What is not in the slice, and why.** The three items that need a decision
rather than a transcription: the document-open `/AA` walk (`DocumentAActions`
depends on it), firing timers (needs `advance_time`, and M14's D14 named
where), and `CommitOutcome::display` reaching the appearance generator —
which the brief's own closing note warns is `LiveInput`-adjacent and delicate
(`docs/status/M14-gaps.md`). Those stay with a Claude author.

---

## Closing note, 2026-09-02 — the numbers step 1 never measured

Step 1's author was cut off mid-verification: the scoring run, `scripts/ci.sh`
and the workspace suite were all in flight and none of them recorded a result.
Everything above stands as written; this section adds the measurements and
nothing is withdrawn.

### The gates

| gate | result |
|---|---|
| `cargo nextest run --workspace` | `Summary [ 127.114s] 3803 tests run: 3803 passed (2 slow), 1 skipped` |
| `cargo test --doc --workspace` | 164 doctests, 0 failed |
| `cargo clippy -p pdfrum-form --features script --all-targets -- -D warnings` | clean. Run separately because `scripts/ci.sh`'s clippy is default-features, so it never lints the `script` module at all |
| `scripts/ci.sh` | **red at the rustdoc step**, for two intra-doc links neither of which is step 1's — see below. Every step before it is green: fmt, clippy `-D warnings`, `cargo nextest run` (3803/3803 again), doctests |

**The two rustdoc failures, and whose they are.** `RUSTDOCFLAGS="-D warnings"
cargo doc` fails on:

```
error: public documentation for `is_leap_year` links to private item `day_from_year`
error: could not document `pdfrum-script`
error: public documentation for `PasswordEncoding` links to private item `try_password`
error: could not document `pdfrum-crypt`
```

Both pre-date step 1 and both are outside it. `crates/pdfrum-script/src/time.rs`
carries the first, byte-identical, since `d74800a` — the AF* crate's own
landing commit, an ancestor of step 1's first commit `af363b5`. The second is
in `crates/pdfrum-crypt`, the A31 slice that arrived through `b144fed`'s sweep
(recorded at `2c6f496`). Neither is touched here: they are one-line
visibility-or-link fixes, but they belong to the crates their authors own and
fixing them from this seat would put someone else's file in this commit. **CI
was already red on this step when step 1 began**, which is itself worth
recording — the gate has been reporting a real defect that no one has read.

Rustdoc precedes four more steps, so `set -e` meant none of them ran. Each was
therefore run standalone, and each is green: `cargo deny check` (*advisories
ok, bans ok, licenses ok, sources ok* — one `license-not-encountered` warning
for an unmatched `Unicode-DFS-2016` allowance, which is not an error), the
pure-Rust tree check (no `-sys`, `cc`, `cmake`, `pkg-config` or `bindgen`
beyond the two named GPU exemptions), `check-no-wgpu.sh`, and
`check-no-boa.sh`. **So the gate's only red is the two intra-doc links, and
nothing step 1 landed is implicated in it.**

The default build still resolves without an engine: `check-no-boa.sh` passes
all four of its assertions across all twenty workspace members, including the
fourth — that `pdfrum-form --features script` *does* reach `boa_engine`, so
the other three are not vacuous.

### The transcript score: 11/47 byte-exact

Measured against `release/pdfrum-tool --features script --js-transcript`, with
the `.in` fixtures expanded by the oracle's own `fixup_pdf_template.py` and
diffed the way `testing/tools/text_diff.py` does (Python text mode,
`readlines`, terminators significant).

**11 of 47 byte-exact:** `bug_1335681`, `bug_1445426`, `bug_695826`,
`bug_740166`, `bug_895152`, `bug_959274_1`, `console_methods`, `unsupported`,
`util_bytetochar`, `util_printx`, `util_scand`.

That is the honest step-1 number, and it is *below* what the accounting above
projects, for a reason the accounting already names: the per-assertion count
(385/387 for the `AF*` fixtures) is not a per-fixture count. A fixture is
byte-exact only if **every** line matches, so one unbound `Doc` method early in
a 400-line transcript costs the whole fixture. `public_methods` is the extreme
case — 291 of its assertions are answerable by `pdfrum-script` today, and it
scores zero because its first line never prints.

Where the other 36 sit:

| bucket | count | fixtures |
|---|---|---|
| `Doc`/`Field` object model — follow-on slices 1 and 2 | 16 | `annot_properties`, `bug_1098213`, `bug_361`, `bug_421304870`, `bug_492_1`, `bug_494057`, `bug_679642`, `bug_735912`, `bug_983867`, `bug_1358075`, `bug_1447268`, `document_methods`, `document_properties`, `field_methods`, `field_properties`, `listbox_methods` |
| the `/AA` event path — no `/OpenAction` at all, so nothing fires | 5 | `bug_1142688`, `bug_1314658`, `event_properties`, `mouse_events`, `public_methods` |
| the rest of `app`, `Annot`/`Icon`, `constructor` — follow-on slices 3 and 7 | 4 | `app_methods`, `app_properties`, `constructor`, `icons` |
| `color` — follow-on slice 4 | 2 | `color_methods`, `color_properties` |
| `global` interceptors — follow-on slice 5 | 1 | `globals` |
| the constant namespaces — follow-on slice 6 | 1 | `consts` |
| `app.execMenuItem` / named actions | 1 | `named_action` |
| **not achievable by construction** (already accounted above) | **5** | `apply`, `array_buffer`, `immutable_proto` (V8's own exception strings); `v8_features`, `foreground_task` (V8's `--expose-gc`, the test harness's flag and not PDFium's) |
| `util_printd` — oracle bug #5 plus overflow-constructed dates | 1 | `util_printd` |

**Nothing here is a new decline.** The 5 not-achievable fixtures are exactly the
five §"A fourth and fifth" already ruled on, and the other 31 are ordinary work
with a named owner: 24 of them fall inside the follow-on slice as carved
(16 + 4 + 2 + 1 + 1), 5 are the `/AA` walk the slice explicitly *excludes* as
needing a decision, 1 is `named_action`'s menu dispatch, and 1 is
`util_printd`'s residue, itself already itemised.

### What the number means against the exit criterion

PLAN §M15 asks for **47/47 accounted for, 44 byte-exact + 3 by construction**;
§"A fourth and fifth" above amends the second half to **42 + 5**. Step 1
delivers **47/47 accounted for and 11 byte-exact**. The gap is not a
re-bucketing — 42 remains the ceiling and 5 remains the decline count — it is
**31 fixtures of object-model boilerplate that step 1 deliberately did not
write**, since step 1's remit was the cascade, the engine, `util`, `app.alert`
and the `AF*` globals. The exit criterion is a milestone criterion, not a
step-1 criterion, and M15 does not close until the follow-on lands. This
section is the measured baseline the follow-on moves from.

### Two things step 1 left unfinished, found by running it

- **`--time` is accepted-and-ignored by `pdfrum-tool`** (`pdfrum-tool:
  --time=1399672130 accepted but not implemented`), while the conformance
  harness passes it in `determinism_args` and `util_printd`'s golden is dated
  by it. The engine's clock comes from `ScriptConfig` instead, which is why
  `util_scand` and `util_printx` still pass; but a reader would reasonably
  expect the flag to be the single source of that instant, and today it is not.
  Naming it rather than fixing it: the flag lives in `pdfrum-tool`, which the
  concurrent `#js-transcript` cluster owns this week.
- **An unbound method silently truncates a transcript.** `bug_421304870` calls
  `this.getAnnots()`, throws, and prints *nothing at all* — not even a
  diagnostic — where the oracle prints one line. The stub rule ("never a panic,
  always a Diagnostic") holds for stubs that exist; a name that was never bound
  is a `ReferenceError`/`TypeError` that aborts the script. That is upstream's
  behaviour too, so it is not a defect — but it does mean the score is
  *step-shaped*: each `Doc` method bound is likely to flip several fixtures at
  once rather than one.

---

## Both of those two things, fixed — 2026-09-02

The closing note above ends by naming two defects and declining to fix either.
Both are now fixed, one commit each, and the second one's investigation
changed what "fixed" means for it.

### `--time` is now the single source of the scripting clock

It was listed in `ACCEPTED_SWITCHES` as parsed-and-ignored while
`conformance::oracle::determinism_args` passed `--time=1399672130` on every
invocation and `ScriptConfig::for_goldens()` hard-coded the same seed a layer
below. Two sources that agree by coincidence are not one source, and a reader
who changed the flag would have watched nothing move.

The flag is now read the way `pdfium_test` reads it
(`testing/pdfium_test/pdfium_test.cc:778-788`): `std::stringstream >> time_t`
extraction, so `--time=x` is the **epoch** rather than an error — extraction
failure leaves the target at zero, which passes the only range check there is —
and `--time=99abc` is 99, while a duplicate and a negative are each refused
with upstream's own message. `Options::time` is `Option<u64>` whose `None` is
upstream's `time_t = -1` sentinel and means the real wall clock.

`for_goldens()` is gone, replaced by `ScriptConfig::frozen_at(seconds)` and
`ScriptConfig::wall_clock()`. The seed is the caller's; `pdfrum-form` no longer
knows which instant a golden run wants, and `PDFIUM_TEST_CLOCK_MS` became
`PDFIUM_TEST_CLOCK_SECS`, exported for tests and a harness rather than applied
here. The timezone hooks now install **only alongside a frozen clock**, which
is upstream's single guard: `FSDK_SetTimeFunction` and
`FSDK_SetLocaltimeFunction` are set together inside `if (options.time > -1)`
(`:2129-2135`). An embedder without the flag therefore gets the machine's real
zone as well as its real clock, where before it silently got Los Angeles.

**What `--time` reaches beyond the JS clock**, checked rather than assumed —
the complete consumer list of PDFium's overridable `g_time_func`/
`g_localtime_func` (`core/fxcrt/fx_extension.cpp:98-120`):

| consumer | ours? |
|---|---|
| the AcroForm JS date helpers (`fxjs/fx_date_helpers.cpp:38,54,196`) → `util.scand`, `util.printd`, `AFDate_*` | yes, this is the one that matters |
| XFA FormCalc's date built-ins (`fxjs/xfa/cfxjse_formcalc_context.cpp:546,1667,2158`) | no XFA |
| `FPDF_CreateNewDocument`'s `/Info /CreationDate` (`fpdfsdk/fpdf_editpage.cpp:204-218`) | we do not create documents from nothing |
| `FPDFDoc_AddAttachment`'s `/Params /CreationDate` (`fpdfsdk/fpdf_attachment.cpp:300`) | no attachment writing |
| the XFA month-calendar's "today" highlight (`xfa/fwl/cfwl_monthcalendar.cpp:450`) | **the one rendering effect**, and XFA |

And what it does **not** reach, which was the question worth asking:
`--show-metadata` is unaffected — `DumpMetaData` calls `FPDF_GetMetaText` for
`CreationDate`/`ModDate` and prints the strings **already stored in the file**
(`testing/pdfium_test/dump.cc:258-274`), never consulting the clock. Neither
annotation `/M` nor a signature time is affected either: PDFium writes
neither, and there is no `FXSYS_time` anywhere in `core/fpdfdoc/`.

One thing worth recording because it is counter-intuitive: `--time` does **not**
reach V8's own clock. No V8 mock-time hook is installed anywhere, so in the
oracle `new Date()` and `Date.now()` read the *real* wall clock while
`util.printd` and `AFDate_*` read the frozen 2014 one. `public_methods`'s
`AFParseDateEx(1, 2) = 1399672130000` is a *PDFium*-clock line, which is why
it pins. Ours freezes boa's clock, which covers both paths — a superset, and it
produces the same bytes because the goldens only ever assert the PDFium-clock
side. A fixture asserting a bare `new Date()` would be non-deterministic
upstream and is not in the set.

### An uncaught exception is now reported, not swallowed — and the oracle is the one with the bug

The closing note called this "upstream's behaviour too, so it is not a defect."
Reading both references says otherwise: it *is* upstream's behaviour, and
upstream is wrong.

**PDFium drops the error on the floor.** `CJS_Runtime::ExecuteScript` returns a
`std::optional<IJS_Runtime::JS_Error>` carrying the message, the line and the
column (`fxjs/cfxjs_engine.cpp:600-622`, `fxjs/ijs_runtime.h:26-32`);
`CJS_EventContext::RunScript` propagates it (`fxjs/cjs_event_context.cpp:49`);
and `CPDFSDK_FormFillEnvironment::RunScript` — the `void` funnel that *every*
document, page and field action goes through — discards it under a standing
`// TODO(dsinclair): Return error if RunScript returns a IJS_Runtime::JS_Error.`
(`fpdfsdk/cpdfsdk_formfillenvironment.cpp:1280-1286`). `OnCalculate` and
`OnFormat` consult `has_value()` to suppress a write-back
(`fpdfsdk/cpdfsdk_interactiveform.cpp:306-309, 338-341`) but never surface the
text, and `CJS_App::RunJsScript` ignores it (`fxjs/cjs_app.cpp:440`). Nothing
is on the `IPDF_JSPLATFORM` or `FFI_` path — `app_alert` is reachable only from
an explicit `app.alert()` (`fxjs/cjs_app.cpp:275`) — so `pdfium_test` prints
**nothing at all**, and `testing/tools/test_runner.py`'s `_VerifyEmptyText`
then scores a fixture that crashed on line 1 as a *pass* when it has no
expected file. **`[oracle-bug]`.**

**pdf.js is the counter-example**, and it is what "correct" means. Each action's
evaluation is individually wrapped, the error is serialized with its message
*and* stack and sent out as a `{command: "error"}` message
(`src/scripting_api/field.js:542-561`, `src/scripting_api/doc.js:192-206`,
`src/scripting_api/app_utils.js:24-27`), and the viewer routes it to
`console.error` (`web/pdf_scripting_manager.js:316-322`). Because the `try`
sits *inside* the `for (const action of actions)` loop, **the next action still
runs** — which is also PDFium's control flow, since `RunDocumentOpenJavaScript`
is `void` and `ExecuteDocumentOpenAction` walks every `/Next` regardless
(`cpdfsdk_formfillenvironment.cpp:1000-1018`). Only the `visited` cycle guard
and a script-destroyed field abort a cascade. So the cascade was never the
defect; the silence was.

pdfrum therefore takes pdf.js's rule. `ScriptFailure` pairs the *whence* with
the stop and renders one line carrying the engine's message and the position
boa puts inside it — the same information upstream's `JS_Error` carries as two
`int`s and then throws away — trimmed to its first line so boa's stack frames
cannot break a caller's one-line-per-failure format. `drain_diagnostics` still
records the kind on the bounded `Diagnostics` sink and now *returns* the
detail, because `DiagKind::ScriptFailed` can say that a script threw but not
which one or what it said, and a bounded sink is the wrong place to grow
strings a hostile file controls.

The tool writes those lines to **stderr, never stdout**. That is the whole of
how the two obligations coexist: stdout is the oracle's transcript and
`testing/tools/text_diff.py` diffs the whole of it, so matching PDFium's empty
stdout keeps every fixture byte-exact, while stderr carries what the oracle
should have written down. **The transcript is the oracle's stdout; the
diagnostic is ours.**

`bug_421304870` still prints nothing on stdout — `getAnnots` is object-model
work, and belongs to the follow-on slice — but the silence is no longer silent:

```
$ pdfrum-tool --js-transcript --time=1399672130 bug_421304870.pdf
(stdout: empty)
Processing PDF file bug_421304870.pdf.
script /OpenAction: TypeError: not a callable function (unknown at :1:25)
```

Note what boa's message does and does not say. `this.getAnnots` is *undefined*,
so this is a `TypeError` on the **call** rather than a `ReferenceError` on the
name — the message carries the position and not the callee. That is what the
engine said, and it is reported verbatim; inventing a name boa did not give
would be worse than the position it did.

**This does not move the score.** `bug_421304870` needs `getAnnots` to print
`Alert: 1`, and this commit binds nothing. What it changes is that the next
person to run the fixture learns why it is empty in one line instead of
bisecting for it — and the note above's own observation still holds: each `Doc`
method bound is likely to flip several fixtures at once, so the score stays
step-shaped.

---

## Step 2, 2026-09-03 — the `Doc`/`Field` object model, and two defects closed

Three commits. The score moves **11/47 → 23/47 byte-exact**, and the board
moves **1514 → 1526 pass** with `js-transcript` 33 → 21 and **no row moving
the other way**.

### The two `pdfrum-form` defects WP12 measured, closed first

Both are behaviour changes, and both were measured on the board as one: a
field-by-field diff of all 1757 rows reports **0 moved, 0 vanished, 0
appeared**. Neither defect was reachable from a golden — no fixture in the set
has a format script whose output differs from its input *and* gets far enough
to commit, and none has a multi-page form with a `/CO` — which is what made
them defects a scoring run could not have found.

**A format script's output was computed and dropped.** `CommitOutcome::display`
had no reader. Upstream does not store it either, because upstream regenerates
immediately — `AfterValueChange` is `ResetFieldAppearance(pField,
OnFormat(pField))` (`fpdfsdk/cpdfsdk_interactiveform.cpp:588`) and the
optional reaches exactly one line, `pEdit->SetText(sValue.value_or(pField->
GetValue()))` (`fpdfsdk/cpdfsdk_appstream.cpp:1752`). This crate hands
appearances back rather than painting them, so the answer has to survive until
the next generation: `FormSession::formatted` is that, keyed by **field**
because `ResetFieldAppearance` gives every control of the field the same
string.

Three distinctions the map has to make, each with a test in
`crates/pdfrum-form/tests/cascade_wiring.rs`:

| case | answer | why |
|---|---|---|
| the field is **focused** | draw the raw value | the form filler's editor is seeded from `GetValue()`, so a user clicking into a currency field edits `1234` |
| the commit ran and the formatter said `None` | **erase** the old string | `std::nullopt` reaches `value_or` as the raw value |
| the commit **never ran** — unchanged, or refused | leave it alone | `AfterValueChange` runs on a *change*; `CommitOutcome::formats()` is that question asked directly, because `display: None` alone cannot tell the last two apart |

**`FieldRef::index` conflated two index spaces.** `page::read` allocates a
`FieldId` per page as that page's `/Annots` are walked; `/AcroForm /CO`,
`Doc.numFields` and `Doc.getNthFieldName` all count positions in the
document's flat terminal-field list. They are the same number only for a
single-page form whose widgets appear in `/Fields` order, and `commit_field`
was spending a calculation's writes as `FieldId(index)` — so on any other file
a calculation would silently overwrite the field the user had just edited.

`WidgetInfo::field_index` now carries the document-wide position beside the
page-local id, `PageForm::field_of_index` converts back, and `FieldRef::index`
is that position. It is `Option<u32>` because not every widget is in the
form's list — an unnamed one has no position, which is the oracle's answer too
(`GetFieldByDict` returns null and `CountFields` never counted it). The
fixture that pins it is one transposition: `/Annots` is `[Typed, Total]` and
`/Fields` is `[Total, Typed]`.

### 2. The `Doc` and `Field` tables

Counted from `fxjs/cjs_document.cpp` (1538 lines) and `fxjs/cjs_field.cpp`
(2897 lines), read end to end rather than sampled.

#### `Doc` — 32 properties, 42 methods

| shape | count | what we do |
|---|---|---|
| **methods, real** | 17 | implemented from the installed model |
| **methods, no-op returning success** | 23 | **one shared body** — upstream's own behaviour, not a stub of it |
| **methods, throw unconditionally** | 2 | the oracle's own message |
| **properties, real getter** | 20 | implemented |
| **properties, `undefined` getter** | 12 | a bare `return Success();` upstream |
| **properties, setter accepted and discarded** | 20 | 12 bare, plus the 8 metadata ones commented `// Read-only.` that nonetheless succeed |
| **properties, setter throws** | 8 | `Cannot assign to readonly property.` |

**Implemented (17 methods):** `getField`, `getNthFieldName`, `getAnnot`,
`getAnnots`, `gotoNamedDest`, `calculateNow`, `resetForm`, `removeField`,
`addIcon`, `getIcon`, `mailDoc`, `mailForm`, `print`, `submitForm`,
`getPageNthWord`, `getPageNumWords`, and `pageNum`'s navigating setter.

**Stubbed-success (23, one body):** `addAnnot`, `addField`, `addLink`,
`closeDoc`, `createDataObject`, `deletePages`, `exportAsFDF`, `exportAsText`,
`exportAsXFDF`, `extractPages`, `getAnnot3D`, `getAnnots3D`, `getLinks`,
`getOCGs`, `getPageBox`, `getURL`, `importAnFDF`, `importAnXFDF`,
`importTextData`, `insertPages`, `removeIcon`, `replacePages`, `saveAs`,
`syncAnnotScan`. **These are not shortcuts**: `addField`'s entire C++ body is
`// Not supported, but do not return an error.` and `return
CJS_Result::Success();`, and `document_methods_expected.txt` asserts
`= undefined` for each with no arguments and with four.

**Declined with reason (4):**

| name | reason | citation |
|---|---|---|
| `getPrintParams` | throws `Operation not supported.` **upstream** | `cjs_document.cpp` |
| `getPageNthWordQuads` | the same, after its permission ladder | `cjs_document.cpp` |
| `getPageNthWord` | the words need a content-stream text extraction this object has no model for; **the range check runs anyway**, because it comes first upstream and its message is what three golden lines assert. Answering a made-up word would be worse than answering nothing | — |
| `getPageNumWords` | the same | — |

`print` and `submitForm` are implemented and **refuse**, and it is the oracle
that refuses them: `IsUserGesture()` is false for a script no click provoked,
which a headless run always is. `User gesture required.` is upstream's answer,
not a decline of ours.

#### `Field` — 52 properties, 26 methods

| shape | count |
|---|---|
| getters that read something | 48 |
| setters that **validate then discard** | 32 |
| setters that write | 11 |
| setters that throw | 7 |
| methods, real | 10 |
| methods, no-op success | 6 |
| methods, throw unconditionally | 10 |

**The 32 validate-then-discard setters are the interesting class**, and they
are why the file is mechanical rather than deep: `set_fill_color` checks the
field exists, checks the permission and checks the argument is an array — and
then returns success without writing. The validation *is* the observable
behaviour, because the errors are what `field_properties_expected.txt`
asserts; the write was never there to reproduce.

**Real writes (11):** `value`, `display`, `hidden`, `readonly`, `rect`,
`borderStyle`, `lineWidth`, `print`, `currentValueIndices`, `delay`,
`exportValues`'s validation.

**Real methods (10):** `getItemAt`, `checkThisBox`, `isBoxChecked`,
`isDefaultChecked`, `defaultIsChecked`, `buttonGetCaption`, `buttonGetIcon`,
`browseForFileToSubmit`, `setFocus`, `getArray`.

**Stubbed-success (6, zero validation upstream):** `buttonImportIcon`,
`clearItems`, `deleteItemAt`, `insertItemAt`, `setAction`, `setItems`. The
consequence worth naming: **a choice field's option list is immutable from
JavaScript**, and `setAction` silently discards every action a script assigns.

**Declined with the oracle's own message (10 methods + 7 setters):**
`buttonSetCaption`, `buttonSetIcon`, `getLock`, `setLock`,
`signatureGetModifications`, `signatureGetSeedValue`, `signatureInfo`,
`signatureSetSeedValue`, `signatureSign`, `signatureValidate`; and the setters
for `defaultStyle`, `doc`, `name`, `numItems`, `type`, `valueAsString`
(`Operation not supported.`) and `page` (`Cannot assign to readonly
property.` — the one inconsistency, reproduced because the golden asserts it).

#### Five readings that took more than transcription

1. **`getField`'s name resolution is a two-stage disagreement.** The gate is
   `CountFields` on the *uncollapsed* name (`cjs_document.cpp:267`); the
   collapse happens afterwards inside `AttachField`. `CFieldTree::FindNode`'s
   walk **stops at the first empty segment**
   (`cpdf_interactiveform.cpp:578-585`), so `MyField..nonesuch` finds the
   `MyField` node where `MyField.nonesuch` finds nothing — one dot apart, and
   that is the whole difference between an object and `undefined`.
   `AttachField`'s collapse is **one pass, not a fixed point**, which is why
   `MyField...nonesuch` keeps a double dot. All ten rows of
   `field_methods_expected.txt`'s table hold.
2. **A name may be a whole subtree**, and it answers the first leaf under it:
   `GetFieldAtIndex(0)` walks the node, so `getField('MyField')` resolves to
   `MyField.MyText` while `.name` still reads `MyField` — the object carries
   the *lookup* name, not the field's.
3. **`Field.value` is coerced through JavaScript's own `Number(s)`**
   (`CJS_Runtime::MaybeCoerceToNumber`, `cjs_runtime.cpp:218-244`), with `NaN`
   allowed only for the literal `"NaN"`. Hence `" 4"` is 4, `"0x100"` is 256,
   `"Infinity"` is a number and `"INFINITY"` is not — the 49 assertions of
   `bug_361`, which a hand-written parser gets wrong in three places.
4. **A list box walks the whole array; everything else takes `strArray[0]`**
   (`cjs_field.cpp:474-525`). `['bar','qux']` on a single-select reads back
   `qux`, and `['foo',1]` reads back `foo` because `FindOption("1")` is `-1`
   and selecting nothing changes nothing. `FindOption` matches the **export
   value alone**, so the label `Foo` selects nothing.
5. **`gotoNamedDest` says nothing at all when it succeeds.** It navigates
   through `FFI_DoGoToAction`, an `FPDF_FORMFILLINFO` callback, and **not**
   the `IPDF_JSPLATFORM` `Doc_gotoPage` that `Doc.pageNum` uses — and
   `pdfium_test` sets the second and not the first. Only the *failure* is
   observable, which is why `bug_1358075`'s golden is one alert and no
   `Goto Page:` line.

#### Three more harness constants, named rather than assumed

They join `--time=` and are the same category: values that are the *test
runner's*, not the PDF's, and that leak into the expected bytes.

| constant | value | where |
|---|---|---|
| `GOLDEN_FILE_PATH` | `myfile.pdf` | `ExampleDocGetFilePath`, `pdfium_test.cc:368-369` |
| `GOLDEN_RESPONSE` | `No` | `ExampleAppResponse` writes two UTF-16 units, `:356-364` |
| the global's string | `[object global]` | V8's global class name; here `Symbol.toStringTag` |

### 3. The seven owed regressions — and what five of them actually test

The step-1 record listed all seven as "crash-and-alert regressions over
`Doc`/`Field` mutation", each needing "`this.getField` plus one or two `Field`
properties". **Read at the line, five of them are timer tests and touch no
field at all.** Every one drives `EmbedderTestTimerHandlingDelegate` and
asserts on what `AdvanceTime(1000)` fires:

| test | `fpdf_formfill_embeddertest.cpp` | what it asserts | needs |
|---|---|---|---|
| `Bug620428` | `:1251-1264` | a cancelled timer and interval fire nothing over 5 s | `advance_time` |
| `Bug634394` | `:1266-1285` | cancelling from inside a callback stops at two alerts | `advance_time` |
| `Bug634716` | `:1287-1305` | the same, a different file | `advance_time` |
| `Bug679649` | `:1307-1320` | a timer the host refuses to create fires nothing | `advance_time` |
| `Bug707673` | `:1322-1336` | a click after `DoOpenActions` fires nothing | `advance_time` |
| `Bug765384` | `:1338-1347` | `setFocus` + `borderStyle`, and nothing dies | **ported, passes** |
| `Bug1477093` | `:1349-1359` | `getField` on a name the form lacks, and nothing dies | **ported, passes** |

So the blocking dependency for the five is `advance_time` — which M14's D14
reserved as the step function and which nothing calls yet — **not** the object
model. The inventory's estimate was wrong in a way only reading the tests
could show; it is corrected here rather than worked around.

`Bug765384`'s crash class is **unreachable by construction** here rather than
by a guard: it was a `CJS_Field` holding a raw pointer into a widget layer
`setFocus` re-entered, and a `Field` here carries a `/Fields` position and
never a pointer. `Bug1477093`'s is the same shape.

The portable half of the five *is* tested — the scripts parse and run,
`setTimeOut` and `setInterval` are recorded rather than fired, and
`clearTimeOut`/`clearInterval` neither throw nor fire anything. A viewer
running those five files shows nothing and stays up, which is precisely what
all five oracle tests assert, arrived at without a clock.

**2 of 7 ported and passing; 5 owed to `advance_time`, re-bucketed with the
reason.** That is a re-bucketing, and it is one the evidence forces rather
than one that makes a number come out: the five were never object-model work.

### The score, per fixture

**23 of 47 byte-exact**, up from 11. The twelve that moved: `annot_properties`,
`app_methods`, `app_properties`, `bug_1098213`, `bug_1358075`, `bug_361`,
`bug_421304870`, `bug_492_1`, `bug_983867`, `document_properties`,
`field_methods`, `listbox_methods`. **None moved the other way.**

Where the other 24 sit:

| bucket | count | fixtures |
|---|---|---|
| **not achievable by construction** (unchanged) | 5 | `apply`, `array_buffer`, `immutable_proto` (V8's own exception strings); `v8_features`, `foreground_task` (V8's `--expose-gc`) |
| the `/AA` event path — no `/OpenAction`, so nothing fires | 4 | `event_properties`, `mouse_events`, `public_methods`, `bug_1142688` |
| `color` — slice 4, untouched here | 2 | `color_methods`, `color_properties` |
| the constant namespaces — slice 6, untouched | 1 | `consts` |
| `global` interceptors — slice 5, untouched | 1 | `globals` |
| `constructor` — the `illegal constructor` shape for eleven bound objects | 1 | `constructor` |
| timers, which need `advance_time` | 1 | `bug_1447268` |
| `Field.delay`'s document-wide half | 1 | `bug_494057` |
| `Icon`'s prototype identity under `new` | 1 | `icons` |
| `Doc.getPageNthWord`'s text extraction | 1 | `document_methods` |
| `Field`'s residue — `lineWidth`/`textFont`/colour setters | 1 | `field_properties` |
| a damaged file the parser reads differently | 1 | `bug_1314658` |
| `util_printd` — oracle bug #5 plus overflow dates | 1 | `util_printd` |

**Nothing here is a new decline.** The five not-achievable fixtures are exactly
the five already ruled on; the other 19 are ordinary work with a named owner.

### `[oracle-bug]` sites in this step

**None new.** Every divergence from `cjs_document.cpp` and `cjs_field.cpp`
found in this step was a *reproduction* rather than a correction, and the two
that looked like candidates were checked and are not:

- **`Field.rect`'s getter and setter disagree about component order** — the
  getter answers `[left, top, right, bottom]` and the setter reads
  `(left, bottom, right, top)`. It looks like `f.rect = f.rect` would swap two
  sides, and it does not: `CFX_FloatRect` is **normalized**, which sorts each
  pair, and the getter re-emits in its own order. The round trip is stable,
  `field_properties`'s four `rect` lines prove it, and there is nothing to
  correct.
- **`Field.defaultIsChecked` validates its argument and then ignores it**,
  answering only "is this a toggle at all" rather than the default state. That
  is a real trap, but `isDefaultChecked` — the correctly-spelled twin, one
  letter away — answers the real question, so the API is not *missing* an
  answer and a correction here would make a golden fail for no gain. Named in
  `field.rs` beside the reproduction rather than filed.

A31's `[oracle-bug]` in `commit.rs` (a refused validate keeps focus) and the
`ScriptFailure` one (an uncaught exception is reported) both still stand and
are untouched.

---

## Step 3, 2026-09-03 — the `/AA` event path, timers, and the last four slices

Four commits. The score moves **23/47 → 33/47** byte-exact, and the board
moves **1526 → 1536 pass** with `js-transcript` 21 → 11 and **no row moving
the other way** at any of the four.

| commit | score | board | rows |
|---|---|---|---|
| the `/AA` event path | 23 → 26 | 1526 → 1529 | `event_properties`, `mouse_events`, `public_methods` |
| `advance_time` + the page `/AA` walk | 26 → 27 | 1529 → 1530 | `bug_1447268` |
| `color`, the namespaces, `global`, `constructor` | 27 → 32 | 1530 → 1535 | `color_methods`, `color_properties`, `constructor`, `consts`, `globals` |
| `words()` | 32 → 33 | 1535 → 1536 | `document_methods` |

### 1. `event` had to become a real object before anything else could work

The `event` a script saw was a **plain data record**, written by a generated
setup script (`event.name = "Format"; event.value = "…";`) and read back with
`context.eval("String(event.change)")`. That shape cannot produce what the
goldens assert, and the gap is not a detail: of `event`'s twenty properties,

- **thirteen throw on assignment** — a non-writable data property is a
  *silent* no-op in sloppy mode, and every one of those lines asserts a thrown
  `Operation not supported.`;
- **`fieldFull` throws on read** outside a Keystroke, with upstream's own bare
  `unrecognized event`, which is not in the `JSMessage` table at all;
- **`selStart`/`selEnd` read `undefined`** outside a Keystroke and *silently
  drop* an assignment there;
- **the three `rich*` names** read `undefined`, accept any assignment, and
  keep reading `undefined`.

So each of the twenty is now an accessor pair over `EventState`, the host
record `CJS_EventContext` keeps. The population table is transcribed from
`fxjs/cjs_event_context.cpp:146-285` — eleven `On*` functions, each naming
exactly which fields it makes live.

#### Two readings that were wrong, both in the expected bytes

**`event.rc` resets to `false`, not `true`.** `Initialize` sets `rc_du_` false
(`:307-308`) and only Keystroke, Validate and Calculate point the getter at a
caller's live flag (`:235,257,272`); everything else reads the dummy. So a
Format script reads `event.rc == false` before touching it, which
`event_properties_expected.txt` asserts on its own first `rc` line — and it
was the single line between that fixture and passing.

**`AF*` reads the host record, never the JavaScript property.** The C++ reads
`field_full_`, `SelStart()` and `SelEnd()` straight off the event context
whatever kind it is, where the *property* throws or answers `undefined`. Going
through the property made every `AF*` call on a Format event throw
`unrecognized event` — which is the whole of `public_methods`'s first test,
372 of its 400 lines.

### The event-object population table

Which fields each trigger makes live. Everything not marked is at its
`Initialize` reset value, and **every trigger resets every field** before
writing its own.

| trigger | `name` | `value` | `change` | sel | `rc` | `willCommit` | `commitKey` | modifiers | `source` |
|---|---|---|---|---|---|---|---|---|---|
| `/AA /E` | `Mouse Enter` | — | — | — | dummy | — | −1 | yes | — |
| `/AA /X` | `Mouse Exit` | — | — | — | dummy | — | −1 | yes | — |
| `/AA /D` | `Mouse Down` | — | — | — | dummy | — | −1 | yes | — |
| `/AA /U` | `Mouse Up` | — | — | — | dummy | — | −1 | yes | — |
| `/AA /Fo` | `Focus` | live | — | — | dummy | — | −1 | yes | — |
| `/AA /Bl` | `Blur` | live | — | — | dummy | — | −1 | yes | — |
| `/AA /K` | `Keystroke` | live | live | live | live | caller's | **0** | yes | — |
| `/AA /V` | `Validate` | live | live | — | live | — | −1 | yes | — |
| `/AA /C` | `Calculate` | live | — | — | live | — | −1 | — | **live** |
| `/AA /F` | `Format` | live | — | — | dummy | **true** | **0** | — | — |

"dummy" is `rc_du_`, which reads `false` and whose writes reach nothing.
`targetName` is set by all ten. `type` is `Field` for all ten. `fieldFull`
throws on read for all but Keystroke.

### When each of the six new triggers fires

Read out of `fpdfsdk/formfiller/cffl_interactiveformfiller.cpp`, and the
**order is observable** — a document with all six alerts them in exactly this
sequence:

| event | fires |
|---|---|
| pointer moves onto a widget | `/X` for the one it left, then `/E` for the one it entered |
| a move within one widget | neither |
| left button down | `/D`, **before** focus moves (`:172-211`) |
| left button up | `SetFocusAnnot` → `/Fo`, **then** `/U` (`:213-250`) |
| double click | `/Fo` again, even on the field that already holds focus |
| the `focus` verb | `/Fo` |
| focus lost | **nothing** — see below |

**`/AA /Bl` never fires from a click that lands elsewhere**, and that is
upstream's behaviour rather than an omission here: `OnKillFocus` returns at
`GetFormField(pWidget)` when the widget has no `CFFL_FormField` yet, and
`mouse_events.evt` has a comment saying so — *"This should trigger an On Blur
event. (Bug 881)"* — beside an expected transcript that contains no `blur`
line. The trigger is wired and reachable; nothing in the fixture set reaches
it.

### Loading a page runs its formatters

`CPDFSDK_PageView` calls `OnLoad` on every annotation it builds, and
`CPDFSDK_Widget::OnLoad` runs `OnFormat()` for every text field and combo box
(`fpdfsdk/cpdfsdk_widget.cpp:1104-1122`). The display string is dropped again
for a text field — only a combo box regenerates from it — and **everything the
script asked the host to do on the way there is not dropped**. That is why
`event_properties` and `bug_1142688`, neither of which has an `/OpenAction`,
print at all.

`FormSession::load_page` is that call, and it is idempotent: a page already
read does not run its formatters twice.

### The harness was missing the sibling `.evt`

`testing/tools/test_runner.py`'s `TestText` runs `pdfium_test --send-events`
**unconditionally** for every javascript fixture and copies `<test>.evt` next
to the generated PDF first (`:658-680`). Four of the 47 carry one, and their
expected text is a *record of those events*. The `js-transcript` scorer now
does the same copy, and `--js-transcript` loads a sibling `.evt` without the
`--send-events` flag for the same reason.

### `submitForm` and `print` stop being unconditional refusals

Both are gated on `IsUserGesture()`, true for exactly Mouse Down, Mouse Up and
Keystroke (`cjs_event_context.cpp:316-325`). The same call is therefore a
`User gesture required.` from one trigger and a submission from another, and
`mouse_events` asserts both halves on one file.

Past the gate, `submitForm` serialises an **FDF file** or a URL-encoded query,
byte-exact against `CFDF_Document::WriteToString`: CRLF line endings, no
whitespace inside the dictionaries, and keys in `std::map` order — `/F` before
`/Fields`, and `/F` before `/Type` before `/UF` inside the filespec. Two
details cost bytes and were found by diffing them:

- the filespec carries **`Doc.URL`'s spelling, not `Doc.path`'s** — both come
  from `GetFilePath()` and it is `Doc.path` that prepends the separator, so the
  FDF reads `myfile.pdf` and the golden's byte count is 174 and not 176;
- **the URL-encoded form is not escaped at all** — `name_b << "=" << csValue_b`
  with an `&` between, so a value containing `&` produces a query that cannot
  be parsed back. Reproduced, because the escaping belongs to whoever posts
  the bytes and correcting it would change what a host receives with no
  evidence about what a receiver expects.

`print` expands its eight **keyword parameters** from a lone object argument
(`ExpandKeywordParams`, `fxjs/js_define.cpp:65-95`), which is why
`this.print({"bUi": false, …})` prints `bUI` as *true*: the object spells the
key `bUi` and `bUI` is not found.

### Six arity checks were the wrong comparison

`arity` was `args.len() < expected` throughout; the oracle writes
`params.size() != n` for most of the family and `< n` for three. So
`AFDate_Format(1, 2)` was accepted where the golden asserts a throw. The table
is now transcribed per function:

| shape | functions |
|---|---|
| `!= n` | `AFNumber_Format` (6), `AFPercent_Keystroke`… no — see below |
| `< n` | `AFNumber_Keystroke` (2), `AFPercent_Format` (2), `AFSpecial_KeystrokeEx` (1) |

and `AFPercent_Keystroke` **is** `AFNumber_Keystroke` under another name, so
it takes the loose check too — which is also why the alert it raises names the
other function. Everything else is `!= n` at the count its own body checks.

Three more in the same commit:

- **`AFPercent_Format`'s `bPercentPrepend` defaulted true** where
  `params.size() > 2 && ToBooleanReentrant(params[2])` makes it **false**,
  putting the `%` on the wrong end of all 186 of `public_methods`'s percent
  lines.
- **`AFMergeChange` reads no argument and requires exactly one** — the
  parameter is Acrobat's `event`, ignored, but the arity check runs first.
- **`AFTime_KeystrokeEx` throws `AFDate_KeystrokeEx`'s bespoke message** under
  its own name, because it *is* that function called through.

### `AFSimple_Calculate` is implemented, now that there is a form to read

It was declined for want of a field lookup. The arithmetic was always
`pdfrum-script`'s; what was missing is turning names into numbers, and the
object model answers that now. What each field type contributes is not "its
value":

| type | contributes |
|---|---|
| text, combo box | the trimmed value read as a number — `abc` is zero, not a failure |
| check box, radio button | the export value of the first checked control |
| list box | its value **only** when at most one row is selected |
| push button, signature | nothing |

and every field reached still **counts**, contributing zero — which is what
makes an average over a text field and a push button divide by two.

### 2. `advance_time`, and where the clock lives

M14's D14 reserved a step function and nothing called it.

**The host owns the clock, and that is upstream's arrangement rather than a
simplification of it.** `FPDF_FORMFILLINFO::FFI_SetTimer` is an embedder
callback and `GlobalTimer` only records the id it returns, so PDFium has no
clock either; its own tests drive one through
`EmbedderTestTimerHandlingDelegate`'s fake elapsed count and expiry-keyed
multimap.

`FormSession::advance_time(Duration) -> usize` and
`ScriptCascade::advance_time` are that seam. Nothing in this library starts a
thread or reads a wall clock, and a session nobody advances fires nothing
however long it lives.

Four rules from `fxjs/global_timer.cpp`, each with a test:

1. **A timer is re-armed before it fires**, not after — so a callback
   cancelling its own timer cancels the *next* firing.
2. **A timer fires at most once per call**, however large the increment: the
   reinsertion is at `now + interval` after the whole step. The oracle's tests
   say so in a comment and prove it by calling `AdvanceTime(1000)` five times
   rather than `AdvanceTime(5000)` once.
3. **A one-shot with a zero timeout never runs its script** —
   `TimerProc`'s `!IsOneShot() || GetTimeOut() > 0`, reachable from one
   argument because `setTimeOut` passes its interval as both the elapse and
   the timeout.
4. **A timer already firing does not re-enter** (`Trigger`'s `processing_`).

The four `app` functions gained their real arity and validation: one or two
arguments and never more, an empty script refused with `The input value is
invalid.`, a **1000 ms** default interval, and `clearTimeOut`/`clearInterval`
taking exactly one argument and ignoring anything that is not a timer — which
is what lets a cancel of a *refused* arm do nothing instead of throwing.
`fail_next_timer` is `SetFailNextTimer`, the only way to reach that branch.

**The `.evt` grammar has no timer verb.** `SendPageEvents` dispatches eight
mouse and keyboard commands and nothing else
(`testing/pdfium_test/event.cc:163-195`), so the seam is on the session and
the tool gains nothing.

#### The five V8 regressions are ported and pass

`Bug620428`, `Bug634394`, `Bug634716`, `Bug679649`, `Bug707673` — each as its
own test, driving `advance_time` the way the oracle's drives `AdvanceTime`.
With `Bug765384` and `Bug1477093` already passing from step 2, **7 of 7 of
that bucket are ported**.

#### `bug_1447268` was mis-filed and is not a timer test

Step 2 listed it under timers. It needs the **page-level `/AA` walk**:
`FORM_DoPageAAction(…, OPEN)` after a page loads and `(…, CLOSE)` before it is
left (`fpdfsdk/fpdf_formfill.cpp:918-944`), off the *page* dictionary — where
`/C` is **Close** and not Calculate, the two sharing a key in different
dictionaries. `FormSession::page_opened`/`page_closed` are those.

It also needed one ordering fix in `Field.checkThisBox`: upstream coerces
`params[0]` to an `int32` on its **first line**, eight lines before the
field-type check, so an argument carrying a `valueOf` runs its callback even
on a text field. Type-checking first lost all six of that fixture's callbacks,
which are the whole of its expected output.

### 3. The four object-model slices, all five fixtures first try

**`color`.** Three readings the goldens turn on: an unrecognised destination
space is **transparent**, so `convert(x, 'BOGUS')` answers `T`; `equal`
compares in the **richer** space rather than component-wise, so a grey equals
the RGB it promotes to; and the twelve names are **variables**, so
`color.black = [...]` is read back and a document that overwrites one has
overwritten it for every script after it.

**The nine constant namespaces** plus ten `IDS_*` strings and twelve `RE_*`
arrays, transcribed from their `JSConstSpec` tables. The `% s` spacing and
`exists.Field` running two sentences together are in the C++ literals.

**`global`** is an interceptor rather than a property list —
`DefineObjAllProperties` installs callbacks for *every* name — so it is a
proxy with six traps. Its three surprises: a delete is a **tombstone** (the
name reads `undefined`, is not enumerated, refuses `setPersistent`, and an
assignment **revives** it); assigning `undefined` **deletes**; and enumeration
is **sorted**, because the bag is a `std::map<ByteString, …>`, with
`setPersistent` ahead of it as a method on the target.

One trap found by running it: the `get` trap must **fall through to the
target** for a name the bag does not carry. The fixture calls
`global.hasOwnProperty(name)` inside its own enumeration loop, and answering
`undefined` for every unbagged name makes that a `TypeError` that swallows the
whole dump.

*Nothing is persisted, and that is exact rather than a shortfall.*
`CJS_Global`'s constructor passes a hard-coded `nullptr` delegate
(`cjs_global.cpp:178-181`) and both the load and the save return false on
their first line when it is null (`cfx_globaldata.cpp:263-266`). The
RC4-encrypted store is dead code upstream; building it would give this library
its only file-writing path for a feature the oracle does not have.

**`constructor`.** Every static bound object throws the bare string
`illegal constructor` when called and `not a dynamic object` when `new`ed
(`cfxjs_engine.cpp:186-196`); a timer, being `FXJSOBJTYPE_DYNAMIC`, refuses
only the plain call. It has to be a **source-level** function rather than a
bound native one: `new` on a `NativeFunction` throws boa's own
`TypeError: not a constructor` *before the body is entered*, which is a
message about the engine rather than the one the golden asserts, and
`new.target` is what tells the two calls apart.

### 4. `words()` — a different reading of "word" from extraction's

`pdfrum_text::words` walks the page's **text objects** and their raw character
codes, and does not go through `TextPage` at all. Extraction reorders by
reading order, suppresses duplicate overprinted objects, inserts generated
spaces and newlines and normalizes what it emits — every one of those would
change the count. The scripting API's list is defined on the content stream as
written, which is what `CPDF_TextObject::CountWords` walks.

The rule that separates two words is one line, and it is not what its name
suggests: `IsLatinWord` is `unicode != 0x20 && unicode <= 0x28FF`, so a run of
characters *below the cutoff* is one word whatever they are. `Hello, world!`
is **two** words and not four, because the comma and the exclamation mark
continue the run.

Two behaviours reproduced rather than corrected: a **word index past the end
answers the last word** (the walk breaks on `>=` and then indexes relative to
the object it stopped in), and **`bStrip` defaults to true**.

`DocumentModel::page_words` is installed by the caller like everything else in
the model — counting words needs a parsed content stream and the cascade holds
no PDF — so this extends the existing install seam rather than handing the
cascade a document.

### `[oracle-bug]` sites in this step

**None new.** Every divergence found was a reproduction, and the four that
looked like candidates were checked and are not:

- **`/AA /Bl` never fires from a click elsewhere** (Bug 881). It is a real
  defect and it is *already filed upstream* — the fixture's own comment cites
  the number. Correcting it here would make `mouse_events` fail on lines the
  oracle does not print, and the trigger is wired, so a document that reaches
  it through a path the fixture does not exercise still gets its script.
- **The URL-encoded submission is unescaped.** Correcting it would change what
  a host receives with no evidence about what a receiver expects; the escaping
  belongs to whoever posts the bytes, and this library posts none.
- **`getPageNthWord` past the end answers the last word.** A bounds check
  would answer `""` where a script gets text — a *different* wrong answer, not
  a right one.
- **`print`'s keyword expansion is case-sensitive**, so `bUi` is silently not
  `bUI`. That is JavaScript property lookup doing exactly what it says; the
  fixture spells it wrong on purpose to pin the behaviour.

### A sixth not-achievable-by-construction fixture

`bug_1142688` deliberately exhausts the call stack — `try { f3(); } catch(e) {}`
recursing — and then does work on the way out, expecting the stack overflow to
arrive as a **catchable** exception. V8 throws a `RangeError`, which the
fixture catches, and the script reaches its final `app.alert('Done.')`.

**boa's `RuntimeLimitError` is uncatchable by construction.** `is_catchable`
is `self.as_engine().is_none()` (`boa_engine-0.22.0/src/error/mod.rs:818-822`)
and every `RuntimeLimitError` is an engine error, so the `catch` is never
entered and the script stops where the limit fired.

**That is the sandbox property, not a defect in it.** The M15 E2 settlement's
whole claim is that exhausting a limit *refuses* rather than accepting, and a
catchable limit error is a limit a hostile script can drive the engine past by
wrapping the loop in a `try`. Making this fixture pass would mean giving that
up. It joins the five already accounted for.

### A seventh, and it took a fix to prove

`bug_679642` looked like ordinary work: its second alert is inside an
`Array.prototype[1]` **setter** that the script installs, provoked by reading
`Field.page`, and the alert fires *before* the `gc()` call two lines below it.
So the question was whether the alert was reachable at all.

It was not, and the reason was a real defect: `Field.page` built its array
with a bulk construction, which **skips the prototype chain**. Upstream fills
it element by element with `PutArrayElementReentrant` — an ordinary `Set` —
so a script that has installed an accessor for one of those indices sees its
setter run while the array is being built. Fixed, and `Firing ...` now fires.

What remains is one line: `gc is not defined`. So `bug_679642` is the same
`--expose-gc` decline `v8_features` and `foreground_task` already carry, and
it is banked as one **only after the reachable half was made to work** —
which is the difference between a decline and an excuse. The fix costs
nothing on the board and is right independently of this fixture: any script
that watches an array being filled was being lied to.

**So the exit restates: 47/47 accounted for, 40 byte-exact and 7 not
achievable by construction** — `apply`, `array_buffer`, `immutable_proto`
(V8's own exception text), `v8_features`, `foreground_task`, `bug_679642`
(V8's `--expose-gc`, the test harness's flag and not PDFium's), and
`bug_1142688` (a catchable stack overflow, which our sandbox deliberately
does not have).

### The score, per fixture

**33 of 47 byte-exact**, up from 23. The ten that moved: `bug_1447268`,
`color_methods`, `color_properties`, `constructor`, `consts`,
`document_methods`, `event_properties`, `globals`, `mouse_events`,
`public_methods`. **None moved the other way** at any of the four commits.

Where the other 14 sit:

| bucket | count | fixtures |
|---|---|---|
| **not achievable by construction** | 7 | `apply`, `array_buffer`, `immutable_proto`, `v8_features`, `foreground_task`, `bug_1142688`, `bug_679642` |
| `Field.setFocus` is recorded and never spent — `take_focus_request` has no caller | 1 | `bug_735912` |
| `app.execMenuItem` / `/Names /Print` named actions | 1 | `named_action` |
| `Field.delay`'s document-wide half | 1 | `bug_494057` |
| `Icon`'s prototype identity under `new` | 1 | `icons` |
| `Field.style` and the colour setters — `Field`'s residue | 1 | `field_properties` |
| a damaged file the parser reads differently | 1 | `bug_1314658` |
| `util_printd` — oracle bug #5 plus overflow dates | 1 | `util_printd` |

**Owed after this step**, in the order a follow-on would take them:

1. **`Field.setFocus` reaches nothing.** `take_focus_request` is drained by
   no caller, so a script moving focus moves nothing and the outgoing field's
   `/AA /Bl` never runs. It is the one gap here that is a *missing wire*
   rather than a missing feature, and it is `bug_735912`'s whole story.
2. `Field.style` and the three colour setters (`field_properties`).
3. `Icon`'s prototype identity, and `Field.delay`'s document-wide half.
4. `app.execMenuItem` and the `/Names /Print` named-action dispatch.
5. **The facade installs no document model.** `FormSession::with_scripts`
   builds a cascade and never calls `set_document`, so `this.getField` in a
   facade session answers as an empty document would — the *tool* installs
   one and the facade does not. Not reachable from any fixture, because the
   scoring path is the tool's, which is exactly why it went unnoticed.

---

## Step 4, 2026-09-03 — the two missing wires, and the four residues

Three commits. **33 of 47 transcript fixtures byte-exact became 37 of 47**,
the board moved **1536 → 1539** with `js-transcript` **11 → 8**, and no row
moved the other way. `form-events` is unmoved at 8.

| commit | what | board |
|---|---|---|
| `8bc4a19` | `[spec]` — `Field.setFocus` reaches the focus machinery | unmoved |
| `84f9a2f` | the facade installs the document model | unmoved |
| `bd6f451` | the four residues: `style`, `Icon`, `delay`, named actions | **1539 / 218**, `js-transcript` 8 |

The first two commits close *dead seams* and move nothing, which is the point:
neither had a fixture that could fail. That is why they were still open after
three steps of fixture-driven work, and it is the same finding step 1 recorded
about `Cascade` — **a wire nothing reads is indistinguishable from a wire that
works**, and only reading the code finds it.

### Two of step 3's five owed items had no fixture on the board at all

Step 3's table named `bug_735912` for `setFocus` and `named_action` for the
named-action dispatch. Both are **suppressed for our build configuration** and
were never scored:

| fixture | `testing/SUPPRESSIONS` | why |
|---|---|---|
| `bug_735912.in` | `* * noxfa *` (:648) | its golden's order needs the XFA focus path |
| `named_action.in` | `* nov8 * *` (:652) | the transcript needs V8 to have run |
| `bug_679642.in` | `* * noxfa *` (:646) | already in the not-achievable bucket |

So the corpus holds 47 javascript templates and the board scores **44** rows.
The "33/47" and "37/47" counts above are over all 47 templates, measured
directly against `<fixture>_expected.txt`; the board's `js-transcript` count
is over the 44 rows it can see. Reconciling them: of the 37 that pass, one
(`named_action`) is suppressed, so the board sees **36 passing of 44** and
reports **8 failing** — which is the `js-transcript` number above.

**`bug_735912` cannot be made to pass and is not a decline we chose.** Its
golden reads `Alert: removed` then `Alert: focused`, and in a non-XFA build
the order is the other way round: at `/OpenAction` time nothing holds focus,
so `SetFocusAnnot`'s kill branch never runs, and the `/AA /Bl` that prints
`removed` only fires later, from `RemovePageView` at `FORM_OnBeforeClosePage`.
The recorded order needs `CPDFXFA_Widget::OnChangedFocus`, which is why the
suppression exists. It is bucketed **not achievable by construction**, like
the V8-string goldens, rather than left as owed work.

### `Field.setFocus`: how the request is routed, and the order proven

`take_focus_request` had no caller anywhere. The request moved from an
inherent method on `ScriptCascade` onto the `Cascade` **trait** (default
`None`), which is what makes it reachable through the `&mut dyn Cascade` the
routing layer holds — `NoScripts` answering `None` is every non-scripting
cascade's honest answer rather than a stub.

`route::focus_field` spends it through the path a click takes, in
`SetFocusAnnot`'s own order:

1. `if (focus_annot_ == pAnnot) return true;` — a request naming the field
   that already holds the keyboard fires **neither** script and moves nothing.
2. `KillFocusAnnot` → `CFFL_InteractiveFormFiller::OnKillFocus` → the outgoing
   widget's **`/AA /Bl`**.
3. `OnSetFocus` → the incoming widget's **`/AA /Fo`**, then the move, which
   commits whatever the outgoing field held.

`route::apply` and `route::kill_focus` drain it on the way out — after the
event's own routing, so the field the event was about has already committed —
bounded at eight moves, because two fields whose focus scripts name each other
are a live-lock upstream recurses into until the stack runs out.

**The order is proven by test, not by fixture**, since the fixture that pins
it is suppressed. `cascade_wiring.rs` gains four, over a page whose `/Fields`
order is the **reverse** of its `/Annots` — so a wire spending the index as a
page-local id would ask for the wrong field and the assertion could fail:

| test | asserts |
|---|---|
| `a_scripts_focus_request_fires_blur_then_focus` | `[(Typed, Blur), (Total, Focus)]`, in that order |
| `a_focus_request_for_the_focused_field_fires_nothing` | the early return |
| `a_focus_request_for_an_unknown_field_moves_nothing` | `GetWidget` answering null |
| `two_fields_asking_for_each_other_terminate` | the bound holds |

A document-level script runs outside any event, so the facade grows
`FormSession::honour_focus_requests` for `/OpenAction`, `/Names /JavaScript`
and a page's `/AA /O`; it reads the page holding the named field if nothing
has, which is `GetWidget` reaching `GetPageViewAtIndex`. The transcript tool
calls it at both points.

### Where the facade installs the model

`FormSession::with_scripts` built a cascade, installed every field's `/AA`
scripts into it, and never called `set_document` — so a script asking the
*document* anything got an empty one. `install_document_model` reads the
catalog, the trailer's `/Info` and the page dictionaries through
`script::model::read` — the same reader the tool uses — and runs at the point
the cascade is attached, **before** `install_calculation_order`. The tool
still installs its own richer model afterwards, so its transcripts are
unchanged, which is why the board did not move.

`with_cascade` is deliberately **not** changed. It boxes its argument into
`Cascades::Plain`, where the installing methods are unreachable, so a
`ScriptCascade` passed there has never had its `/AA` entries or its `/CO`
order installed either — the constructor's own documentation already said so,
and a test now pins it.

Two things stay out of the model on purpose: `path`/`URL`, because no PDF
records the path it was opened from and a conformance run passes the
harness's own `myfile.pdf`; and `page_words`, because counting them parses
every page's content stream and 0 is what an empty page gives.

### The residues

| residue | what the oracle does | what we did | fixture |
|---|---|---|---|
| `Field.style` | first char of `/MK /CA` as a ZapfDingbats selector; **empty caption falls back to the kind** — circle for a radio, check otherwise | implemented; a non-toggle throws `Object is of the wrong type.` | `field_properties` ✅ |
| a toggle's `value` | the checked control's export value, or the literal `Off` | implemented — we gave the raw field value, so a group with no `/V` read `""` | `field_properties` ✅ |
| a toggle's `value =` | `SetCheckValue` unchecks every control whose export value does not match | implemented — a value naming no control leaves the group `Off` | `field_properties` ✅ |
| `Icon` prototype | `FXJSOBJTYPE_DYNAMIC`, so `new icon.constructor()` succeeds and inherits from the icon prototype | gave the shared prototype a constructor whose `.prototype` points back | `icons` ✅ |
| `Field.delay` | the queue is the **document's**; `Doc.delay = true` **clears** it | moved the queue to the host; four drains, not one | `bug_494057` ✅ |
| named actions | `DoActionNamed` → `ExecuteNamedAction`, no JavaScript run | `record_named_action`; the tool's `/OpenAction` walk collects both kinds in one order | `named_action` ✅ (off-board) |

`Field.delay` was the one that needed a shape change rather than a value. The
queue lived on the *JavaScript object*, which can express "this field's writes"
and nothing else; upstream's is `CJS_Document::delay_data_`, which four things
touch — every field pushes into it, `Field.delay = false` drains the entries
naming that field (`DoFieldDelay`), `Doc.delay = false` drains them all, and
`Doc.delay = true` clears it. Only the fourth is `bug_494057`'s assertion, and
only the document-owned queue can express it.

The three colour setters step 3's table paired with `Field.style` turned out
to cost nothing: `field_properties` is byte-exact with `style` and the two
toggle fixes alone, because every field in the fixture is `["T"]` and the
setters are writes the golden asserts are ignored.

### `[oracle-bug]` sites in this step

**None new.** Every divergence found was a reproduction. The two that looked
like candidates were checked and are not:

- **`Field.style`'s setter validates and discards.** `set_style` checks
  `can_set_` and returns success without writing anything, so `style` is
  read-only in fact while presenting as writable. `field_properties` asserts
  exactly that (`testRIProperty` — "write ignored"), so it is the specified
  behaviour rather than a defect, and reproducing it is right.
- **`Doc.delay = true` discards rather than defers.** It reads like a bug —
  a batching flag that loses writes — but `delay_data_.clear()` is
  deliberate and `bug_494057` is the regression that pins it. Reproduced.

### The score, per fixture

**37 of 47 byte-exact**, up from 33. The four that moved: `bug_494057`,
`field_properties`, `icons`, `named_action` (the last off-board). **None moved
the other way.** On the board that is three rows, `js-transcript` 11 → 8.

Where the other 10 sit:

| bucket | count | fixtures |
|---|---|---|
| **not achievable by construction** | 8 | `apply`, `array_buffer`, `immutable_proto`, `v8_features`, `foreground_task`, `bug_1142688`, `bug_679642`, **`bug_735912`** |
| a damaged file the parser reads differently | 1 | `bug_1314658` |
| `util_printd` — oracle bug #5 plus overflow dates | 1 | `util_printd` |

**The exit restates as 47/47 accounted for — 37 byte-exact, 8 not achievable
by construction, 1 parser work, 1 oracle bug.** Of the two remaining, only
`bug_1314658` is ordinary work with a named owner: a damaged file whose
`/OpenAction` our parser does not reach. `util_printd` stays in the oracle-bug
bucket it has been in since the `AF*` review.

### What M15 still owes

Nothing that is a wire. Both seams step 3 left set-but-never-read are spent,
and the sweep that found them — reading for a value nothing consumes, rather
than for a fixture that fails — is worth repeating on the next milestone
rather than at the end of one.

`bug_1314658` is the last board-visible javascript row that is neither a
decline nor an oracle bug, and it is a **parser** row wearing a transcript
row's clothes: the fixture is a deliberately damaged file, and what it tests
is whether the `/OpenAction` survives the damage.

---

## Addendum, 2026-09-06: `util_printd` leaves the oracle-bug bucket

`util_printd` is **byte-exact**, and the accounting above is one row stale in
two directions at once. Both are recorded here rather than rewritten into the
tables, so the reasoning that produced the tables stays readable.

### Oracle bug #5 was fixed upstream

The table's row 5 — `util.printd`'s `dddd`/`ddd` print `Sunday`/`Sun` for
every date — is no longer what the oracle does. `cjs_util.cpp:275` now reads

```cpp
  time.tm_wday = FX_GetDayOfWeekFromTime(date);
```

so the `struct tm` the format string is handed carries the real weekday, and
`util_printd_expected.txt:13-14` in the current checkout assert `Friday` and
`Fri` — the answers the ruling said were correct and that we already produced.
The two lines the bucket held were never *our* deviation; they were a stale
transcript. The bucket for this fixture is empty, and **there is no
`util_printd` line we cannot match**.

### The winter half of the timezone was wrong, and hid until the calendar moved

What replaced it, briefly, was ours. `GOLDEN_TIMEZONE_OFFSET_SECS` was a flat
`GMT-0700`, and §"the timezone `TZ=America/Los_Angeles` names is not the one
`util.printd` uses" above is right about *why* the two offsets differ but
wrong about the second one being a number at all.

The asymmetry is one-sided. `pdfium_test` replaces `FXSYS_localtime` with
`gmtime` (`testing/pdfium_test/pdfium_test.cc:2134`), so
`GetDaylightSavingTA`'s `tm_isdst` is always zero
(`fxjs/fx_date_helpers.cpp:54-67`) and `FX_LocalTime`'s shift really is the
flat `GMT-0800` that `GOLDEN_PRINTD_OFFSET_SECS` records. But **V8 is not
hooked**: the `Date` a fixture constructs resolves the real
`America/Los_Angeles` from the zone database, per instant. So `Date`'s offset
is `GMT-0700` in July, `GMT-0800` in December, and `-7:52:58` — the city's
solar offset — before the railroads standardised time in 1883.

A flat `GMT-0700` is therefore right for the 52 July lines and an hour wrong
for the five that are not:

| line | `new Date(...)` | real offset | why |
|---|---|---|---|
| `:32` | `(1850, 0, 1)` | `-7:52:58` | local mean time, before 1883 |
| `:33` | `(2525, 11, 31)` | `GMT-0800` | December |
| `:44` | `(0, 06, 04, ...)` → 1900 | `GMT-0800` | July, but before the US kept daylight saving at all |
| `:46` | `(2014, 20, 100, ...)` → 2015-12-09 | `GMT-0800` | December |
| `:51` | `(2014, 01, 30, ...)` → 2014-03-02 | `GMT-0800` | before the March transition |
| `:53` | `(2014, -1, 30, ...)` → 2013-12-30 | `GMT-0800` | December |

Under the old flat `GMT-0700`, five of the six differed: `:33`, `:44`, `:46`,
`:51` and `:53`. Line `:32` matched, because `-7:00` and `-7:52:58` land on
the same side of midnight for a `mm/dd/yyyy` format — which is why adding the
daylight rule without also adding the pre-1883 solar era fixed five lines and
broke that one, printing `01/01/1850` for an expected `12/31/1849`. Both eras
are needed, and the two midnight-boundary lines (`:32` and `:33`) are what
make the difference visible: the other four print the hour, so they read as an
hour early rather than a day.

The board reported only `:33`, because `--check-regressions` records the
**first** differing line.

**Why this looked like it depended on the day the board ran.** It does not,
and that is worth stating plainly, because it was the first hypothesis. The
fixture's dates are all literals; the run is under a frozen clock
(`--time=1399672130`); and nothing on this path consulted `SystemTime::now()`
or the machine's zone before the fix or after it. Two runs of the board from
this worktree are byte-identical, as are two from `main`.

What changed was the row the runner *reported*, not our output. The committed
`conformance/scoreboard.json` still records this row as
`line 13: expected "Alert: dddd: Sunday", got "Alert: dddd: Friday"` — the
long-bucketed oracle bug #5 — while the checkout's
`util_printd_expected.txt:13` now reads `Friday`. When the checkout picked up
the upstream weekday fix, lines 13 and 14 stopped differing, the *first*
differing line moved from 13 to 33, and a row that had been failing for a
known and accepted reason started reporting an unfamiliar one-day-off
mismatch. The one-day-off had been there the whole time, behind it.

### The fix

`crates/pdfrum-form/src/script/zone.rs`: the configured zone is a
`Zone { standard_offset_secs, daylight }` rather than an `i32`, and
`ScriptConfig::timezone` replaces `timezone_offset_secs`.
`GOLDEN_TIMEZONE_OFFSET_SECS` becomes `GOLDEN_TIMEZONE`, which is
`Zone::LOS_ANGELES`. `Daylight::UnitedStates` carries the federal rule in
eras — solar before 1883, none before 1918, first-Sunday-April to
last-Sunday-October to 2006, second-Sunday-March to first-Sunday-November
after — and the mid-century patchwork is deliberately not modelled, because a
table of it is a zone database and no fixture reaches it. `GOLDEN_PRINTD_OFFSET_SECS`
is unchanged: printd's flat `GMT-0800` was always right.

The unit tests pin the far-future and pre-1883 cases by their own instants, so
none of them can pass or fail according to the day the suite runs.
