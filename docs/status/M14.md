# M14 — Form interaction (`pdfrum-form`)

Running status. The brief (`docs/design/pdfrum-form.md`) is the behaviour
contract; SPEC §15 is the shape contract; this file records what landed, what
is blocked, and the questions the milestone had to answer by measurement.

## OQ6 — the caret's blink phase in a golden. **Answered: always drawn.**

Settled before any caret code was written, as the ruling asked, by reading one
golden and one C++ file.

`testing/resources/pixel/form_textfield_focused_ltr_expected.pdf.0.png` is
200×100. Its `.evt` types `abc... def` into the field at (100, 50) and
deliberately never clicks away, so the field is focused when the image is
taken. Scanning the image's dark columns:

- columns 51–99 are the glyph run `abc... def`, all antialiased colour;
- **column 100 is a solid `(16, 16, 16)` bar spanning y 38–51** — fourteen
  rows, four taller than the glyph band (y 40–48) at each end, one pixel wide,
  with column 101 a clean `(238, 238, 238)` and column 102 empty.

That is a caret, drawn, immediately after the final `f`. So a focused field's
`LiveEdit` appearance draws one.

The C++ says why, and says it is not luck. `CPWL_Caret::DrawThisAppearance`
returns early on `!flash_`, and `flash_` is set **`true`** the moment the caret
becomes visible (`cpwl_caret.cpp:97`); the only thing that ever clears it is
`OnTimerFired`, on a 500 ms `CFX_Timer`. `pdfium_test` runs no message loop
that fires it — its `idler()` is empty in a V8-off build — and the determinism
recipe freezes the clock at `--time=1399672130` besides. The caret is therefore
lit on every first render, on every platform, and the phase question the brief
flagged as "the one place the goldens might diverge for a reason we do not
control" does not arise.

**Ruling for the implementation: draw the caret unconditionally whenever a
field is focused with an empty selection.** Geometry, from `cpwl_caret.{h,cpp}`:
a line of width `0.4` units (`cpwl_caret.h:41`) stroked at
`x = rect.left + width * 0.5` from the caret rect's bottom to its top, in
opaque black, clipped to the client rect. And it is *selection*-gated, not
focus-gated alone: `SetCaretInfo` passes `selection.is_empty()` as the
visibility flag and `CPWL_Edit::SetCaret` forces invisible when
`!IsFocused() || IsSelected()` — **a field with a live selection shows no
caret** (brief §1.20.5 rule 7). The two `form_textfield_selected_*` goldens are
the other side of that switch.

## U2 — the hit-test tie-break. **Answered: strict `>` against the half-width.**

The ruling assigned U2 to the `pdfrum-doc` slice, and that slice's agent has
now read it and reported the rule; it is recorded here because
`place_at_point`'s callers in this crate are what an error in it would break.

`CPVT_Section::SearchWordPlaceImpl` is a binary search whose only comparison is

```cpp
if (fx > pWord->fWordX + vt_->GetWordWidth(*pWord) * 0.5f)
```

**Strict `>`, against the word's own x plus half its width.** So a click
exactly on a glyph's midpoint lands *before* that glyph, and the answer is the
place after the last word whose midpoint is strictly left of the click — the
word index staying `-1`, the "before the first word" place, when none
qualifies.

The strictness and the `0.5` both matter: the `InsertTextInPopulatedTextField`
family clicks at x = 134 and expects the caret after exactly four characters,
so an off-by-one here fails four assertions at once. Caret-placement tests in
this crate are written around the midpoint rule rather than around a
nearest-edge one.

## What landed

- `[spec]` SPEC §15 written from brief §3.2/§3.5/§3.6, and `max_undo_items`
  argued onto `SessionConfig` rather than `Limits` (§15.2), with §10's line
  annotated to say where it went.
- The crate skeleton, registered in the workspace between `pdfrum-doc` and the
  facade.
- `event.rs` — the semantic event vocabulary that is `pdfrum-tool`'s mapping
  target (SPEC §15.5). Verified against the harness reviewer's seven bridging
  facts: page-space coordinates with no transform, a key-down with no key-up,
  a `char` the bridge narrows to fallibly, and `modifiers` retained on the
  three events whose script form hardcodes them to zero because the ported
  assertions pass real ones.
- `edit/{place,select,undo}.rs` — the place model, the directional selection
  with its collapsed-versus-reset distinction, and the group-atomic undo
  stack.
- `cascade.rs` — the `Cascade` seam and `NoScripts`, whose method defaults are
  the script-free behaviour rather than a stub of it.
- `session.rs` — the session record and its two platform configurations.
- `tab.rs` — the focus ring, its two upstream seed quirks, and the terminating
  banding of D10.
- `hit.rs` — the hover/click hit-test asymmetry, the layout bands, and the
  focused-first / focused-last orderings.
- `geom.rs` — page↔plate with the rotation normalized once (D3).
- `field/{toggle,button,choice}.rs` — the check box / radio machine, the push
  button's asserted-broken action behaviour, and the combo/list machine with
  its three counter-intuitive rules.

## Divergences kept

As specified in brief §2. Recorded here as they are implemented.

- **D10 — tab-order banding terminates.** Implemented in `tab.rs`. Where the
  oracle's row and column loops `continue` without erasing — and so spin
  forever on a page whose remaining annotations all have a non-positive top —
  the fold appends the remainder in index order and reports `degenerate`. A
  generative test walks 200 random six-annotation geometries in all three
  orders asserting the pass returns and loses no annotation.

## Two tab-order seed rules, verified against a compiled simulation

Both are upstream quirks that change the resulting order, and both were
reproduced only after checking a transcription of the C++ against this
crate's output, because reading either one from the source alone invites the
wrong answer.

1. **A row band's seed is the *rightmost* of the annotations tied for
   topmost.** The scan runs from the highest index down with a strict `>`, so
   a tied annotation never displaces the running best, and after the
   left-ascending sort the first one visited is the rightmost. Two rows of two
   therefore traverse `3, 1, 0, 2`, not the `1, 3, 2, 0` reading order
   suggests. Confirmed by compiling the C++ loop verbatim against the same
   fixture.
2. **The column pass's "nothing chosen yet" guard is `left < 0`, and when it
   fires it seeds index zero** rather than the index being examined. On a page
   with negative x coordinates the guard re-fires on later iterations. Both
   halves are reproduced; this is why the row and column passes are not one
   function with a flipped axis.

## Assertions ported

**73 ported assertion tests passing**, in `crates/pdfrum-form/tests/`, against a target of 191 (121 formfill + 70 pwl/formfiller). Plus 155 unit tests in the crate's own modules — 228 in
`pdfrum-form` altogether, and 317 counting the facade's. The 11 V8-gated names are M15's and are listed in brief
§4.1; they are recorded, not ported.

| File | Tests | Covers |
|---|---|---|
| `tab_order.rs` | 7 | the five `FormFill*Tab` tests over `annotiter.pdf`'s real geometry |
| `keyboard.rs` | 13 | `DoNotHandleShortcutsOnKeyDown`, `DoNotHandleSelectAllOnChar`, `SelectAllWithOnKeyDown`, `UndoWithOnKeyDown`, `RedoWithCtrlYKeyboardShortcut`, `KeyPressWithNoFocusedAnnot`, the read-only consumption rule, the delete-character filter |
| `undo_shape.rs` | 9 | `UndoRedo` ×2, `ContinuouslyReplaceAndKeepSelection`, `CutAllTextUndoRestoresAllCharacters`, `ReplaceSelection`, `ReplaceAndKeepSelection` step 9, the queue-limit pair, the selection asymmetry |
| `choice_fields.rs` | 9 | `SetSelectionProgrammaticallyMultiSelectField`, `CheckForNoOverscroll`, the `/I`-versus-`/V` trio, `SetSelectionProgrammatically{NonEditable,SingleSelect}Field`, `BadApiInputs*`, `FocusChanges` type-ahead |
| `never_panics.rs` | 9 | brief §4.4's P8, plus the two termination properties the oracle cannot state |

| `text_editing.rs` | 20 | `UndoRedo`, `ReplaceAndKeepSelection`, `ReplaceSelection`, `CutAllTextUndoRestoresAllCharacters`, `RedoCutSelection`, `SelectAllText`, the five `DeleteTextField*`, and the **eight-way character-limit family** |
| `acceptance_click.rs` | 6 | `InsertTextInPopulatedTextFieldMiddle` on the real fixture geometry, `DoubleClickInTextField`, drag selection, and the caret-or-bands overlay rule |

**How the remaining 153 divide.** They are not 153 independent pieces of work:

- The **text-editing clusters** (B, and the reading half of C) assert a
  field's text and selection after typing, clicking and dragging. Every one of
  them needs the `vt` place queries, and they will port in a block once those
  land.
- The **appearance clusters** (E, and the eight `insta` snapshots) need the
  `LiveEdit` branch, which needs a caret rectangle, which needs the same
  queries.
- The rows that could be ported without them have been.

Two upstream rows are recorded as **not portable, deliberately**: the index
`−1` cases in `BadApiInputs*` cannot be written against an unsigned index, so
the misuse they guard is unreachable here. That is a reduction in reachable
API misuse, not a dropped assertion.

## Verification

- `cargo nextest run` over the whole workspace: **3291 passed, 1 skipped**.
- `cargo test --doc --workspace`: all green.
- `cargo clippy --workspace --all-targets`: no warnings.
- `cargo fmt -p pdfrum-form -- --check`: clean. (Deliberately per-crate:
  other agents hold uncommitted work in this tree.)
- The conformance scoreboard is untouched by this slice — nothing here reaches
  the rendering path.

Workspace tests went from 3291 to 3304 with this slice's additions; every one
of the pre-existing 3291 still passes.

## The facade, and what a bridge can rely on

`pdfrum::FormSession` is in, with the documented example the exit criterion
asks for, and it is **stable enough to bridge onto**. Three shapes are settled
so the `--send-events` wiring does not have to move later:

- coordinates are **page space**, y-up, with no conversion on the way in;
- there is **no key-up method** — the oracle's is a documented permanent no-op
  returning false, so modelling it would only invite callers to send a dead
  event — and the `.evt` `keycode` verb's down/up pair maps to one `KeyDown`;
- the **right button is reachable** through `on_button` rather than absent,
  because scripts contain right-button lines and the correct behaviour for
  them is to consume nothing.

Mouse and keyboard dispatch currently report every event unhandled, and the
method docs say why rather than leaving a bare stub: routing needs the per-page
annotation geometry and the caret-placement queries. **"Not consumed" is a
promise a bridge can rely on not to change meaning** when routing lands — a
guess would not be.

## Dispatch wiring — `pdfrum-tool --send-events` onto the facade

`--send-events` no longer only parses and counts: `crates/pdfrum-tool/src/dispatch.rs`
bridges `pdfrum_tool::events::Event` onto `pdfrum::FormSession`, and `run.rs`
replays the whole script against every page before that page's image is
written. The result is **inert-correct**: the facade's dispatch reports every
event unhandled by design, so the wiring changes no pixel yet — and that is
the property this slice is scored on.

### The seven bridging facts, and where each one lives

The harness reviewer's list (`docs/reviews/claude-review-evt-harness.md`) is
binding, so each fact is named at the code that implements it rather than only
here.

| # | Fact | Where |
|---|---|---|
| 1 | Coordinates are page space, y-up — **no transform** | `dispatch::at`, an `i32`→`f32` widening and nothing else |
| 2 | `keycode` is the down/up pair → **one** `on_key_down`, the up dropped | `dispatch::to_call`'s `KeyCode` arm; `Call` has no `KeyUp` variant to hold one |
| 3 | `CharCode.code` is an `i32` code point, narrowed **fallibly** | `dispatch::to_char`, with the skip announced on stderr by `replay_page` |
| 4 | Modifiers pass through | `dispatch::to_modifiers`, the grammar's three bits onto the semantic ones |
| 5 | Never repair unbalanced mouse events | `to_call` holds no state at all, so there is nothing that *could* synthesize the missing edge |
| 6 | Replay the whole stream once per page, before saving that page's image | `run::walk_pages`, after the page-load guard and ahead of every dump |
| 7 | Goldens are script-keyed already | nothing to do; the harness landed it in `d72e8ac` |

Two decisions inside those worth stating, because a later reader would
otherwise assume the opposite:

- **An invalid code point is skipped, never replaced.** `charcode,55296` is a
  lone surrogate; substituting U+FFFD would type a visible glyph the oracle
  never typed. The skip goes to **stderr**, because stdout is the
  byte-compared stream.
- **`Call` is a named intention, not a direct call.** The facade's dispatch is
  inert, so a test driving a real session cannot tell a correct bridge from one
  that sends nothing. `to_call` is a pure function of the grammar event, which
  is what makes the key-pair collapse, the skipped code point, the untouched
  mouse imbalance and the verb-for-verb ordering all assertable without a
  document. `perform` is a total match over it, so a variant cannot be added
  without a facade call being wired.

### The order reproduced from `pdfium_test`

`PdfProcessor::ProcessPage` (`pdfium_test.cc:1474-1482`) sends events as its
**first** statement, ahead of every dump and every render; a page that would
not load returns before reaching it. `GetPage` (`:871-872`) runs
`FORM_OnAfterLoadPage` then `FORM_DoPageAAction(OPEN)` on first load, and the
render is `FPDF_RenderPageBitmap` with `FPDF_ANNOT` (`:1130`, flags from
`:221`) followed by `PageRenderer::Finish`'s `FPDF_FFLDraw` over the same
bitmap (`:1046`). **`FORM_ForceToKillFocus` is never called** — I grepped the
whole of `testing/pdfium_test/` — which is why the focused fixtures are
expected to render their live editor state rather than a committed appearance.

One session serves the whole document, as the oracle holds one
`FPDF_FORMHANDLE` for one document: what page 0's replay leaves focused is
what page 1's replay starts from. The session is opened from the *same bytes
and same password* as the render path's document, via `Arc<[u8]>` — a session
over a different reading of the file would route clicks against annotations
the renderer never saw.

`Sent N events.` is unchanged and stays oracle-compatible: it counts the
**script's** lines, which is a property of the file, not of how many times the
stream was dispatched or of how many lines dispatch could use.

### What the facade still lacks for the renderer

This is the one thing the slice could not finish inside its write scope, and
it is recorded rather than worked around.

`render::render` now takes the `Vec<AppearanceUpdate>` the replay produced and
carries it to the point of use, but **cannot apply it**. The blocker is
precise:

1. **There is no seam to inject an appearance through.**
   `pdfrum_doc::annot_render::overlay` — the tool's `FPDF_ANNOT` pass, and the
   only place widget appearances reach the page graph — builds its own
   `AnnotOverlay` internally by calling `ap::generate_appearances_with_text`
   over the document. It takes no caller-supplied overlay and offers no
   override hook. Honouring `UpdateKind::Regenerated`/`LiveEdit` means giving
   `overlay` an `Option<&AnnotOverlay>` (or a pre-seeded one) that it merges
   *over* what it generates — a `pdfrum-doc` signature change this slice does
   not own. `AnnotOverlay::set` already exists and is exactly the right
   vehicle, so the change is small; it is simply not mine to make.

2. **The index space of `AnnotId` is not stated to be the overlay's.**
   `AnnotOverlay` is keyed by the **raw `/Annots` array index** — `overlay`
   looks up `list.source_indices[slot]`, not `slot`, and the two differ
   whenever a page has a pop-up, which `AnnotList::load` filters out.
   `session::AnnotId`'s doc says "in load order", which reads as the *filtered*
   index. Until one of the two says which it is, an update cannot be applied to
   the right annotation on any page carrying a pop-up. **This wants one
   sentence on `AnnotId::index`** naming the raw `/Annots` index (or a
   conversion where they differ). Cheap to settle now, silently wrong to guess.

3. **Nothing is blocked on the update *type* itself.** `UpdateKind::appearance()`
   already yields the `&GeneratedAp` that `ap::stream_dict` turns into the same
   form XObject `overlay` builds for a file-supplied `/AP`, so once (1) and (2)
   are settled the renderer needs no new data. `UpdateKind::Cleared` maps to
   "draw nothing generated", which `overlay` expresses by having no entry —
   though a *positive* cleared marker would be needed to suppress a file's own
   `/AP`, and the current enum cannot say that. Worth confirming when routing
   lands; it does not arise while dispatch is inert.

Until then `render` carries a `debug_assert!` that no update holding an
appearance ever reaches it. That is not a stub: it is the alarm that fires, in
tests and in the debug binary, on the first run where dispatch stops being
inert and this section stops being true.

### Scoreboard: unmoved, as intended

`conformance run --check-regressions conformance/scoreboard.json` over the
whole corpus, with the release binaries:

```
run: 1705 files, 1636 pass, 69 fail
  form-events              18
  page-count                2
  pixel-fail               42
  tierA-mismatch            9
  text                 2052/2061 pages (99.6%)
no regressions against conformance/scoreboard.json
```

Diffed field-by-field against the committed board rather than trusting the
totals: **1705 → 1705 rows, 0 added, 0 removed, 0 differing** in status, tags,
tier A, tier B or notes. The scoreboard is therefore **not** re-committed —
there is nothing in it to update.

The `form-events` cluster holds its `1fa7527` baseline exactly, 12 pass / 18
fail, every ssim identical to six decimals. Three of the twelve passes are the
review's finding 4 — real passes on a difference SSIM 0.99 cannot see, marked
`*` — so the count of fixtures genuinely needing no work is nine, not twelve.

| Status | ssim | Fixture |
|---|---|---|
| fail | 0.976851 | `corpus/pdfium/annots/annotation_highlight_author_content.pdf` |
| pass | 0.999999 | `corpus/pdfium/annots/annotation_highlight_empty_content.pdf` |
| fail | 0.924741 | `corpus/pdfium/annots/annotation_highlight_long_content.pdf` |
| fail | 0.978698 | `corpus/pdfium/annots/annotation_highlight_no_author.pdf` |
| pass | 0.999999 | `corpus/pdfium/annots/annotation_highlight_no_author_no_content.pdf` |
| pass | 0.999999 | `corpus/pdfium/annots/annotation_highlight_no_content.pdf` |
| pass\* | 0.991774 | `resources/pixel/bug_113910.in` |
| pass\* | 0.991774 | `resources/pixel/bug_113910.pdf` |
| fail | 0.918875 | `resources/pixel/bug_1372651.in` |
| fail | 0.918875 | `resources/pixel/bug_1372651.pdf` |
| fail | 0.975664 | `resources/pixel/bug_477200528.in` |
| fail | 0.975664 | `resources/pixel/bug_477200528.pdf` |
| fail | 0.978497 | `resources/pixel/bug_736695_2.in` |
| pass | 0.997003 | `resources/pixel/bug_736695_3.in` |
| pass | 0.999998 | `resources/pixel/bug_736695_4.in` |
| pass | 0.999998 | `resources/pixel/bug_736695_4.pdf` |
| pass\* | 0.990167 | `resources/pixel/checkbox_radiobutton.in` |
| pass\* | 0.990167 | `resources/pixel/checkbox_radiobutton.pdf` |
| pass\* | 0.994617 | `resources/pixel/combobox_form.in` |
| pass\* | 0.994617 | `resources/pixel/combobox_form.pdf` |
| fail | 0.926985 | `resources/pixel/form_textfield_focused_ltr.in` |
| fail | 0.930355 | `resources/pixel/form_textfield_focused_rtl.in` |
| fail | 0.930355 | `resources/pixel/form_textfield_focused_rtl.pdf` |
| fail | 0.909175 | `resources/pixel/form_textfield_selected_ltr.in` |
| fail | 0.908047 | `resources/pixel/form_textfield_selected_rtl.in` |
| fail | 0.953963 | `resources/pixel/password.in` |
| fail | 0.953963 | `resources/pixel/password.pdf` |
| fail | 0.986707 | `resources/pixel/scrollable_widgets1.in` |
| fail | 0.986707 | `resources/pixel/scrollable_widgets1.pdf` |
| fail | 0.979377 | `resources/pixel/scrollable_widgets2.in` |

`*` — `bug_113910`, `checkbox_radiobutton` and `combobox_form` pass on a
sub-1% pixel difference the global floor cannot resolve (review finding 4).
The edit-control slice should tighten those three once it can draw them.

## Two contracts the dispatch wiring forced into the open

Both were raised by the `--send-events` agent (`39336fa`) and both were
genuine ambiguities rather than bugs: the code did the right thing, but the
documented contract admitted a second reading that would have been silently
wrong. Both are now normative in SPEC §15.

### `AnnotId::index` is the **raw** `/Annots` index, pop-ups counted

The doc said "in load order", which reads as the pop-up-filtered position.
`pdfrum-doc`'s `AnnotList` drops pop-ups — they are synthesized separately —
so a widget's slot in that list is not its index in the file's array. The
appearance overlay the renderer applies updates through is keyed by
`source_indices[slot]`, the **raw** index. The two agree on every page without
a pop-up, which is what made the wrong reading dangerous: it would have
applied every appearance after the first pop-up to the wrong annotation.

Raw is the natural choice, not a concession — hit testing walks `/Annots`
itself, so it is the index the walk already holds. Verified: this crate
carries `AnnotId` opaquely and never derives an index, so no code changed;
three tests now pin it (a widget at raw index 1 behind a pop-up, the band sort
reordering without renumbering, and the focus ring keeping the gaps that
unfocusable annotations leave).

### `Cleared` → `RevertedToFileAppearance`

It mapped to "no overlay entry", and absence from the overlay already means
"use whatever the file declares" — so the variant could only ever have meant
*revert*, never *suppress*. The name now says so.

Suppression is not merely unimplemented, it is unneeded: `ResetAppearance`
always writes an appearance rather than blanking one, and the widgets that
genuinely draw nothing (signature, and types the classifier cannot name) never
receive interaction state, so they never produce an update at all. If a later
milestone needs real suppression it needs a new variant **and** a positive
marker in the overlay — the doc comment says so, so it is not spelled by
re-reading this one.


## Cross-vendor review: every finding's disposition

The review (`claude-review-form.md`: 1 blocker, 9 should-fix, 6 nits) was
addressed in place. Outcomes, in its numbering:

| # | Finding | Outcome |
|---|---|---|
| 1 | **BLOCKER** — SPEC §15 declares an API the code contradicts on eight points | **Fixed** (`caad342`, `[spec]`). §15.2/§15.3/§15.6 amended to what shipped — `TextState`/`ChoiceState` under their real names, `ToggleState`'s `on_state`, the boxed payloads, `max_calculate_depth`. What has *not* landed is marked as such rather than deleted: `apply`, `FormContext`, `ChoiceState`'s `popup`/`edit`, and eight of the facade's methods. |
| 2 | `seed_index` doc and brief §1.15.2 say "leftmost"; code and tests say rightmost | **Fixed.** Doc comment corrected; the brief gets a dated correction note rather than a rewrite, so the record of what was believed stays readable. The note names the test that would fail if the code were "fixed" to match. |
| 3 | `AnnotId::index` not pinned to an index space | **Already fixed** on main by `061e0c4` + `62c8c75`, as the review notes. |
| 4 | `hit::contains` does not normalize; the C++ `Contains` does | **Fixed.** Normalizes before comparing, so an inside-out `/Rect` is still clickable. A test now asserts the inverted rect *is* hit — the property the never-panics test could not catch, since it only asserted "invents nothing". |
| 5 | The combo box's four-item undo group is neither implemented nor recorded | **Recorded as blocked** (below). It needs `ChoiceState` to carry the editable combo's `TextEdit`, which is the same missing half SPEC §15.3 now marks. |
| 6 | Escape filtered to `Ignore`; upstream consumes it | **Fixed.** `Escape` is returned before the filter and is consumed, read-only or not. `TextAction::Escape` had been declared and never constructed, which is what gave the omission away. |
| 7 | `Home`/`End` gate on the accelerator; the oracle hard-codes Control | **Fixed.** Document-wise motion is Control's on every platform, with the open upstream bug named in the comment. Reproduced rather than improved. |
| 8 | Three accelerator predicates, none matching the oracle's subset test | **Fixed.** One `is_shortcut`, the oracle's subset test, shared by the router and the session. An extra modifier no longer disqualifies a chord. |
| 9 | `undo_shape.rs` asserts granularity its own loops guarantee | **Fixed.** Renamed to what it pins — the walk and eviction — with each test pointing at `text_editing.rs`, which drives the real operations and makes the granularity assertion for real. |
| 10 | The worst-case four-item group at the floor is never built | **Fixed.** The group now has a non-empty removal, so it is the full four items, and the test asserts it exactly fills the floor. |
| 11 | `UndoStack::undo`'s doc says "oldest first" | **Fixed.** Says newest-first, and names the asymmetry with `redo` as deliberate. |
| 12 | Catch-all `_ =>` arms on our own enums | **Fixed.** Exhaustive arms everywhere, including the facade's five `FieldState` matches — the ones that bite, since a new variant should force every query to be revisited. |
| 13 | The facade's "defaults for this platform" are always non-Apple | **Fixed.** The doc says non-Apple and points at `SessionConfig::apple`. |
| 14 | Facade tests pass vacuously when the fixture is missing | **Fixed.** The helper panics with the open error instead of four tests going green-and-empty. |
| 15 | The `/V`-beats-`/I` test invents its mismatch | **Fixed.** Uses `Listbox_MultiSelectMultipleMismatch`'s own five animals, `/V [(Alligator) (Cougar)]` against `/I [1 3 4]`, expecting `{0, 2}`. |
| 16 | `top_visible_for`'s row count is hand-supplied | **Fixed.** Derived from the fixture's `/Rect` height and `/DA` font size, so the no-overscroll property is what is asserted. |

## The `form-events` cluster, re-scored

`conformance run --check-regressions` against the pre-slice scoreboard:

```
run: 1705 files, 1636 pass, 69 fail
  form-events              18
  page-count                2
  pixel-fail               42
  tierA-mismatch            9
no regressions
```

**The 1675 non-form-events rows are unmoved**, and the totals are identical to
the baseline in every tag. **12 of the 30 form-events rows pass; 18 fail**,
every one of them on SSIM below the 0.99 floor rather than on an error — so
the pixel path is end to end and what remains is fidelity, not plumbing.

**Corrected 2026-09-01 (block 2).** The last clause is half right and the
half that is wrong matters. The pixel *delivery* path is indeed end to end —
`annot_render::overlay_with`, the raw-index-keyed `AnnotOverlay`, the tool's
`session_overlay` and `render::render` all work. But nothing is delivered
through it, because dispatch never routes an event; see the withdrawal under
"The 18, by residue". What remains is not fidelity: it is the routing layer,
plus one additive `pdfrum-doc` seam that lets a generator draw a session's
live text instead of the file's stored `/V`.

### The 18, by residue

> **Withdrawn 2026-09-01 by the block-2 agent. The table below is kept
> because it records what was believed, but its diagnosis is wrong and the
> per-row mechanisms it names are not real.**
>
> Every row in it fails for **one** reason: `pdfrum::FormSession`'s
> `dispatch` and `dispatch_keyboard` are inert. Both are still
> `let _ = (...); Response::ignored()` — no event is routed, so no field is
> ever focused, no character is ever typed, and every one of the eighteen
> renders the **plain unfocused appearance**. What the table calls a scroll
> offset, a selection band and a password substitution are not separate
> defects; they are descriptions of the difference between an unfocused
> render and a focused one, which is a single missing mechanism read six
> ways.
>
> **The measurement.** Rendering `scrollable_widgets1.pdf` with
> `--send-events` and diffing the 300x600 output against both of the
> oracle's goldens:
>
> ```
> ours vs oracle events golden : 3000 differing pixels, rows 170-199, cols 100-199
> ours vs oracle plain  golden : 3000 differing pixels, rows 170-199, cols 100-199
> ```
>
> The two goldens differ from *each other* in exactly that region and nowhere
> else. We differ from both identically, which is what "we produced the
> pre-event image" looks like: the events golden there shows a focused list
> box with a dark selection band, a scrollbar and scrolled rows, and we draw
> the unfocused box. A per-row pixel residue would not have this shape — it
> would differ from the events golden and *agree* with neither.
>
> The scores in the table are therefore real, and their ordering is real —
> a row differing in a small widget scores higher than one differing in a
> large one — but the "Residue" column is measuring how much of each page a
> focused widget covers, not what is wrong with it.

| Rows | SSIM | Residue (as believed; see the withdrawal above) |
|---|---|---|
| `form_textfield_selected_{ltr,rtl}` | 0.908–0.909 | The **selection band**: bands are computed per line from caret rectangles, and the oracle fills a single run per line with white text over it. Lowest scores in the cluster, which is consistent with a whole filled rectangle differing. |
| `bug_1372651`, `form_textfield_focused_{ltr,rtl}` | 0.919–0.930 | **Live editor state**: the dropdown's open popup geometry, and the focused field's caret and glyph run. |
| `annotation_highlight_*` (3 of 6) | 0.925–0.979 | **Pop-up on hover** — these are the six bare-`mousemove` fixtures. Three already pass; the three that do not carry longer content, so the residue is the synthesized pop-up's text layout rather than the event path. |
| `password`, `bug_477200528`, `bug_736695_2` | 0.954–0.978 | Password substitution and list-box/combo option rendering while focused. |
| `scrollable_widgets{1,2}` | 0.979–0.987 | **Scroll offset**, closest to the floor of any row. The wheel handler is not wired into dispatch yet, so these render unscrolled. |

None of the 18 is a crash, a panic or a missing page: every row produced an
image and was compared. The two `scrollable_widgets` rows at 0.9867 and 0.9794
are the nearest to clearing, and both need the same one thing — the wheel event
routed to a scroll offset.

## What a reviewer should look at first

Three places where the implementation is deliberately not what a reader would
write, each with the reasoning in the code rather than only here:

1. `tab.rs`'s two seed rules. A row band seeds on the *rightmost* of the
   annotations tied for topmost, and the column pass's "nothing chosen yet"
   guard is a negative-coordinate test that seeds index zero. I got both wrong
   first and only fixed them by compiling the C++ loop against the same
   fixture and diffing; `tests/tab_order.rs` now checks them against
   `annotiter.pdf`'s real coordinates.
2. `commit.rs`'s rejection path, which reports success so that focus proceeds.
   `CommitOutcome` answers "committed" and "reverted" separately rather than
   collapsing them into one bool that would invite the wrong reading.
3. `field/choice.rs`'s three counter-intuitive rules, each with a test that
   would fail if someone tidied it.


## Block 2: routing, and the two seams the pixels then asked for

### What was actually wrong (and the correction above)

The milestone's status said the pixel path was "end to end" and what remained
was "fidelity". The delivery half was indeed complete — `overlay_with`, the
raw-index-keyed `AnnotOverlay`, the tool's `session_overlay` and
`render::render` all worked. But **nothing was delivered through it**: the
facade's `dispatch` and `dispatch_keyboard` returned `Response::ignored()`
without touching the session, so no field was ever focused and no character
was ever typed. See the withdrawal under "The 18, by residue".

### What landed

- **`page.rs`** — the walk that was missing between the machines and the
  events. A page's raw `/Annots` array into `Candidate`s for the hit test,
  `Focusable`s for the tab ring, and per-widget configuration for a field's
  state. Deliberately not `pdfrum-doc`'s `AnnotList`, which drops pop-ups and
  would renumber every widget after one.
- **`TextState` is the edit control.** It was a bare `String` beside a
  detached `UndoStack`, which could disagree — undoing moved one and not the
  other. `ChoiceState` gained the editable combo's own `TextEdit`, which is
  the review's finding 5: the four-item undo group it asks for is a group over
  that control, and there had been nowhere to put it.
- **`route.rs`** — one `match` over an event. Every decision it reaches was
  already a pure function elsewhere; what it owns is the sequencing, plus the
  one decision that is genuinely its own: when a field's interaction state
  comes into being (lazily, on first touch) and when it is written back to an
  appearance (on the way out, never cached).
- **The facade routes.** `dispatch` reads the named page once, keeps it, and
  applies the event. `dispatch_keyboard` routes to the page holding focus,
  which is why typing works after a click and does nothing before one.
- **`crates/pdfrum/tests/form_routing.rs`** — ten assertions that could not be
  written before. Driven against a real `Document` through the public
  surface, every one of them would have passed vacuously against a session
  that consumed nothing.

### The first seam: `LiveState`

A focused field must draw the **session's** text, not the file's `/V`. Every
generator in `pdfrum-doc` read the stored value and offered no override, so
the caret that landed in `e172d63` could only ever be positioned over text the
user had not typed. Requested and landed as `6c672a9` (`ap::field_body::LiveState`
plus `ap::widget::generate_with_live`); `route::generate` is its one caller
here.

### The second seam: the focused widget must not be tinted

**This is the gate the rows are still behind, and it is one unconditional
call.**

`pdfium_test` draws widgets through `CFFL_InteractiveFormFiller::OnDraw`
(`cffl_interactiveformfiller.cpp:59-95`), which branches:

- **lines 69-83** — the widget has a live form-field control: draw its
  appearance, then, if it is the focus annot, `CFX_DrawUtils::DrawFocusRect`,
  then **return**;
- **lines 85-94** — otherwise: draw the appearance, then `DrawShadow`.

`DrawShadow` is the form-field highlight, and it is on the **else** branch
alone. So the widget being edited is **never tinted**.

~~and it gets a dashed black rectangle instead.~~ **Withdrawn the same day,
before it was relied on.** That reads the two halves as one rule and they are
two. The branch is taken on *having a live control*, not on being focused, and
**every** exit inside it skips the tint — including the exit taken when the
focus box comes back empty. Whether anything is *stroked* is then a separate
question, answered by the control's own `GetFocusRect`
(`cffl_formfield.cpp:480-489`), and most controls answer nothing:

| control | focus box |
|---|---|
| text field (`cpwl_edit.cpp:313-315`) | **empty** |
| combo box (`cpwl_combo_box.cpp:321-323`) | **empty** |
| multi-select list box (`cpwl_list_box.cpp:227-234`) | the caret item, clipped to the client area |
| single-select list box, check box, radio (`cpwl_wnd.cpp:713-719`) | the window rectangle inflated by one |
| push button (`cpwl_special_button.cpp:21-24`) | the window rectangle deflated by the border |

So a focused **text field draws no outline at all** — a caret and glyphs over
plain white — which is what all four `form_textfield_focused_*` goldens carry.
The one corpus file that does stroke dashes is `scrollable_widgets1`, a
multi-select list box, and they trace its **caret row inside the widget**
rather than its own edges. Had the withdrawn sentence been implemented, every
focused text field would have gained a box the oracle never draws.

`annot_render::overlay_with` calls `highlight(...)` unconditionally in all
four branches of its draw loop, with no way for a caller to name the focused
annotation.

**The measurement**, on `form_textfield_focused_ltr` — 200x100, widget
`/Rect [50 40 150 70]`, so device rows 30-59 and columns 50-149 — against the
golden `input.pdf.0.events-a631b3ae.png`:

```
        identical pixels inside the widget:    0 of 3000
  our most common colour there: (240, 244, 255)  x2711
  the oracle's most common:     (255, 255, 255)  x2681
```

`(241, 244, 255)` is what this crate's own
`the_highlight_colour_is_bgr_and_composites_to_the_goldens_tint` asserts the
tint composites to over white. We paint it over the focused field; the oracle
leaves that field white. **Every pixel in the widget differs because of the
tint**, which is why the rows did not move when `LiveState` landed — the live
text reaches the page and is then covered.

The glyphs underneath are right: comparing row-ink profiles, our text run and
the oracle's occupy the same columns, and the residual after the tint is a
~1-row vertical offset and antialiasing weight.

### Where routing did move the rows

13 of the 18 moved, several upward, with 0 status changes and all 1705 rows
intact:

| Row | before | after |
|---|---|---|
| `form_textfield_selected_ltr` | 0.909175 | **0.937902** |
| `form_textfield_focused_ltr` | 0.926985 | **0.947667** |
| `form_textfield_selected_rtl` | 0.908047 | **0.913307** |
| `password` | 0.953963 | **0.955176** |
| `scrollable_widgets2` | 0.979377 | **0.980332** |
| `bug_1372651` | 0.918875 | 0.905231 |
| `bug_477200528` | 0.975664 | 0.948919 |
| `form_textfield_focused_rtl` | 0.930355 | 0.907758 |
| `scrollable_widgets1` | 0.986707 | 0.982319 |

The rows that fell are the ones now drawing live editor state *under* the
tint, which is a larger difference than drawing the committed value under it
was. That is expected and is not a regression in the scoreboard's sense: no
row changed status, so the board is not re-committed.

### A residue that is not a bug: the scrollbar

`scrollable_widgets{1,2}` need something neither seam provides. Their goldens
show a **scrollbar**: the selection band stops at column 186 to leave room for
one at columns 187-197, and `CPWL_ScrollBar::kWidth` is `12.0f`
(`cpwl_scroll_bar.h:80`). Nothing in pdfrum draws one — it is
`cpwl_scroll_bar.cpp` and `cpwl_sbbutton.cpp` with `cpwl_list_ctrl.cpp`
behind them, about 1771 lines of widget chrome that the milestone's plan never
accounted for.

**Those two rows cannot reach 0.99 without that subsystem**, and it is named
here as a mechanism rather than left as a number.


## Block 2 close: where the milestone stands

### Assertions ported: 110, from 73

| File | Tests | Covers |
|---|---|---|
| `tab_order.rs` | 7 | the five `FormFill*Tab` tests over `annotiter.pdf`'s geometry |
| `keyboard.rs` | 13 | the shortcut table, both platform branches |
| `undo_shape.rs` | 9 | the stack walk and its eviction |
| `choice_fields.rs` | 9 | the `/I`-versus-`/V` trio, no-overscroll, type-ahead |
| `never_panics.rs` | 9 | brief §4.4's P8 and the two termination properties |
| `text_editing.rs` | 20 | the eight-way character-limit family and the delete/insert rows |
| `acceptance_click.rs` | 6 | `InsertTextInPopulatedTextFieldMiddle` on real geometry |
| **`pwl_edit.rs`** | **12** | the signed-index selection table, the three removal positions, `ReplaceAndKeepSelection`'s reversed case, the eight `SetTextWith*` rows |
| **`special_buttons.rs`** | **6** | `EnterOn{,ReadOnly}{CheckBox,RadioButton}` and the toggle/set asymmetry |
| **`combo_box_edit.rs`** | **8** | the editable combo's text half, and the **four-item undo group** |
| **`form_routing.rs`** (facade) | **11** | click → type → read → undo end to end through the public API |

The four new files are this block's. `form_routing.rs` is the one that could
not exist before: every assertion in it would have passed vacuously against a
session that consumed nothing.

**What the port found.** Two real defects, neither visible from the pixels:

1. **A single-line field's text outran its layout.** The layout drops line
   breaks (`cpvt_variabletext.cpp:249` returns from `InsertSection` unchanged
   when the field is not multiline), but the edit control stored the caller's
   string verbatim — so `"Foo\nBar"` reported seven characters while its
   layout held six, and every index derived from one missed in the other. That
   is a caret landing in the wrong place, not a cosmetic difference.
   `TextEdit::new` now normalizes; a test asserts the two agree across seven
   inputs.
2. **The wheel was implemented as a scrollbar drag.** It is not:
   `CPWL_ListBox::OnMouseWheel` calls the same `OnVK_DOWN`/`OnVK_UP` the arrow
   keys do, and the view follows only when the new row would leave the box.
   The wrong reading left the selection behind on a row that had scrolled out
   of sight.

### The 18 rows, honestly

12 pass, 18 fail, unchanged in count. Every row's SSIM from the pre-routing
baseline to now:

| Row | before | now |
|---|---|---|
| `form_textfield_focused_ltr` | 0.926985 | **0.975161** |
| `form_textfield_selected_ltr` | 0.909175 | **0.968879** |
| `bug_477200528` | 0.975664 | 0.964683 |
| `password` | 0.953963 | **0.960984** |
| `form_textfield_selected_rtl` | 0.908047 | **0.939735** |
| `form_textfield_focused_rtl` | 0.930355 | 0.930445 |
| `scrollable_widgets2` | 0.979377 | **0.985859** |
| `scrollable_widgets1` | 0.986707 | 0.982131 |
| `bug_736695_2` | 0.978497 | 0.979073 |
| `bug_1372651` | 0.918875 | 0.892958 |
| `annotation_highlight_author_content` | 0.976851 | 0.976851 |
| `annotation_highlight_no_author` | 0.978698 | 0.978698 |
| `annotation_highlight_long_content` | 0.924741 | 0.924741 |

**No row crossed 0.99, and the residues are four mechanisms, not thirteen.**

#### Residue 1 — the scrollbar's reserved width (`scrollable_widgets{1,2}`)

Measured SSIM **0.982131** and **0.985859**.

A list box always gets a vertical scrollbar (`cffl_listbox.cpp:33` sets the
style unconditionally), and `CPWL_Wnd::GetClientRect` (`cpwl_wnd.cpp:465-477`)
then **deflates the client rectangle by `CPWL_ScrollBar::kWidth = 12`** on the
right. Every row of text and every selection band is laid out into that
narrowed box: the goldens' bands stop at column 186 of a widget spanning
100-199, and ours run the full width.

Per the milestone ruling the bar itself is **not drawn** in M14 — the brief's
appendix dispositions `cpwl_scroll_bar`/`cpwl_sbbutton` as "behaviour ported,
widget erased". The *reservation* is behaviour and should be honoured, but it
belongs to `ap::field_body::list_box`, which computes its own client rectangle
inside `pdfrum-doc` and is not this slice's to change. Recorded rather than
worked around.

**Reopening condition:** a consumer needing pixel-exact list boxes, or the
`pdfrum-doc` slice taking the 12-unit deflation when a field is a list box or
a scrolling multiline text field.

**A second mechanism, measured afterwards and larger than the first.** A list
row's height is **not** the font size. `CPWL_ListCtrl::Item::GetItemHeight`
(`cpwl_list_ctrl.cpp:49-51`) is the item edit's `GetContentRect().Height()` —
the laid-out line, which is `(ascent - descent) * size / 1000`
(`cpvt_variabletext.cpp:211-212`, `:667-670`). On the golden the selection
band is **14 device rows** at 12-point Helvetica; ours is **12**, so the
ascent/descent pair our layout carries sums to about 1000 where the oracle's
sums to about 1167.

Attributing `scrollable_widgets2`'s 1877 differing pixels exhaustively:

| pixels | mechanism |
|---|---|
| 340 | the 12-unit scrollbar strip, columns 187-198 |
| 374 | the two band rows the oracle has and we do not |
| 1163 | the row *positions* downstream of that height |

Correcting `route::row_height` alone was tried and **reverted** (1877 → 1916,
worse), and the reason is worth recording so it is not tried again:
`ap::field_body::list_box` computes each row's height itself from
`vt::layout(&option.label, …).content_rect_pdf()`, so the height that is
*drawn* is `pdfrum-doc`'s and this crate's only feeds hit-testing and the
scroll clamp. Changing one of the two desynchronises them. The fix belongs in
the `vt` metrics, and both follow from it — which also means the mechanism is
not confined to these two rows: it moves every list box in the corpus.

#### Residue 2 — the open dropdown (`bug_1372651`, 0.892958)

The only row that moved **down** under every change, and the reason is
structural rather than a defect: the golden shows a combo box with its
**popup list open**, drawn *over* the page below the widget. Nothing here
draws an open popup — `CPWL_ComboBox`'s list window is a second window, not
part of the widget's appearance stream — so the more live state we render
correctly inside the widget, the further the page as a whole gets from a
golden dominated by a list we do not draw at all.

#### Residue 3 — the synthesized pop-up's text layout (`annotation_highlight_*`, 0.9247–0.9787)

Three of the six bare-`mousemove` fixtures, and **the only three rows that did
not move by a single digit** under routing, focus gating or the wheel fix.
That is the evidence for what the original doc guessed: these do not exercise
form interaction at all. Their difference is in `pdfrum-doc`'s synthesized
pop-up appearance generator — the three that fail carry longer content than
the three that pass, so it is the text layout inside the pop-up. Untouched
here, and correctly so.

#### Residue 4 — glyph placement inside a correct field (the rest)

`form_textfield_focused_ltr` is the cleanest measurement of what is left. The
widget is 100x30 device pixels; before this block **3000 of its 3000 pixels
differed** (the field was drawn unfocused and then tinted). Now **370 differ**,
and they are confined to rows 38-51, columns 50-101 — the glyph run and the
caret. The tint is gone, the text is the session's, the caret is in the right
column. What remains is a roughly one-row vertical offset and antialiasing
weight on the glyphs themselves, which is the ordinary text-rendering residue
the corpus's non-form rows also carry.

### Verification

- `cargo nextest run`: **3430 passed, 1 skipped**.
- `cargo test --doc --workspace`: all green, including the facade's worked
  example, which now asserts what it documents.
- `cargo clippy --workspace --all-targets -- -D warnings`: clean.
- `cargo fmt -p pdfrum-form -p pdfrum -p pdfrum-tool -- --check`: clean.
- `conformance run --check-regressions`: **1705 files, 1636 pass, 69 fail, no
  regressions**, with the 1675 non-form-events rows unmoved in status, tags,
  tier A, tier B and notes.
- `RUSTDOCFLAGS="-D warnings" cargo doc --no-deps` is clean for these three
  crates. The workspace-wide gate currently fails inside `pdfrum-doc`
  (`overlay_with` links to the private `focus_rect`), which is reported to
  that slice's owner rather than fixed here.


## The exit arithmetic, in three buckets

The criterion is **191 assertions** (121 `fpdf_formfill_embeddertest.cpp` +
70 across the four `pwl`/`formfiller` files). Every one of the 191 falls into
exactly one of three buckets, so the denominator cannot quietly shrink.

### Bucket 1 — ported and passing: **163 tests**

The `pdfrum-form` files drive the machines on values; the `pdfrum` ones drive
the **facade**, which is the surface a bridge calls and the only place the
routing bugs below were visible.

| File | Tests |
|---|---:|
| `pdfrum-form/tests/tab_order.rs` | 7 |
| `pdfrum-form/tests/keyboard.rs` | 13 |
| `pdfrum-form/tests/undo_shape.rs` | 9 |
| `pdfrum-form/tests/choice_fields.rs` | 9 |
| `pdfrum-form/tests/never_panics.rs` | 9 |
| `pdfrum-form/tests/text_editing.rs` | 20 |
| `pdfrum-form/tests/acceptance_click.rs` | 6 |
| `pdfrum-form/tests/pwl_edit.rs` | 12 |
| `pdfrum-form/tests/special_buttons.rs` | 6 |
| `pdfrum-form/tests/combo_box_edit.rs` | 8 |
| `pdfrum/tests/form_routing.rs` | 29 |
| `pdfrum/tests/form_tab.rs` | 11 |
| `pdfrum/tests/form_listbox.rs` | 7 |
| `pdfrum/tests/form_choice.rs` | 6 |
| `pdfrum/tests/form_toggle.rs` | 6 |
| `pdfrum/tests/form_actions.rs` | 5 |

The count is **tests**, not upstream rows, and the two differ in both
directions: a row asserting one rule from eight angles (the character-limit
family) becomes several tests, while a walk asserting a dozen states in
sequence (`FocusChanges`) becomes one.

### Bucket 2 — V8-gated, deferred to M15: **11**

Recorded, not ported, exactly as brief §4.1 lists them: `DisableJavaScript`,
`DocumentAActions`, `DocumentAActionsDisableJavaScript`, `Bug551248`,
`Bug620428`, `Bug634394`, `Bug634716`, `Bug679649`, `Bug707673`, `Bug765384`,
`Bug1477093`.

### Bucket 3 — not portable by construction: **5**, each with its reason

These assert the behaviour of a C out-parameter API against null and negative
inputs. The conditions they guard are **unreachable** here, not unchecked —
which is a reduction in the API's misuse surface rather than a dropped
assertion. Inventing a check would be asserting something about a language,
not about PDF.

| Row | Why it cannot be written |
|---|---|
| `BadApiInputsText` (index `−1`) | `set_index_selected` takes `usize`; a negative index is not expressible |
| `BadApiInputsComboBox` (index `−1`) | as above |
| `BadApiInputsListBox` (index `−1`) | as above |
| `GetFocusedAnnotation` (null handle / null out-params) | `focused_annot()` returns `Option<AnnotId>`; there is no handle to be null and no out-parameter to be null |
| `SetFocusedAnnotation` (null handle / null annot) | as above |

Each row's **other** half is ported: the `index 100` cases are in
`choice_fields.rs`, and the focus rows' behavioural half — that clicking a
widget on each page focuses that page's annotation, and that focus is
document-wide — is `form_tab.rs`'s
`focus_is_document_wide_not_per_page`.

### One metric explains three residues: the font's ascent and descent

The list-box row height, the caret's height and the glyphs' vertical position
are **not three findings**. They are one, and it is worth stating once because
fixing it moves several rows at a time.

Every vertical extent the layout produces is `(ascent - descent) * size /
1000`, from `vt::Metrics`, which `ap::TextFont::metrics_of` fills from
`font.type_ascent()` / `font.type_descent()`. Three independent measurements
say that pair sums to about **1000** here where the oracle's sums to about
**1167**:

| measured on | ours | oracle | implied `ascent - descent` |
|---|---|---|---|
| `scrollable_widgets2`'s selection band | 12 rows | 14 rows | 1000 vs 1167 |
| `password`'s caret, at 12 pt | 24 rows | 28 rows | 2000 vs 2333 (two lines) |
| `form_textfield_focused_ltr`'s glyph run | ~1 row high | — | the same shift |

**Refined 2026-09-01, after `26f3706`.** The inference above is half right and
the correction narrows the search usefully. That commit reports the face
loading as **905 / −211**, summing to 1116 — not the ~1000 the band's height
implies — and the layout computing **13.392 units** per row at 12 points from
it. So the *metric* is close to the oracle's and the **band drawn from it is
not**: 13.392 units come out as **12 device rows** where the oracle inks 14.

The scroll-bar reservation is confirmed fixed and separable: after `26f3706`
our band spans columns **101-186** and so does the oracle's, exactly. What
remains on `scrollable_widgets2` is one number — a row inked 12 device rows
tall against 14 — and it is now the only difference left inside the band.

So the question for the `pdfrum-doc` slice is narrower than "the ascent and
descent are wrong": it is why a row whose computed height is 13.392 paints 12
rows. The caret measurement stands as the independent cross-check, since its
height is one line by construction and carries no other term.

The caret is the clearest of the three because its geometry is unambiguous:
`CPWL_Edit::SetVisibleEditCaret` (`cpwl_edit.cpp:673-695`) sets its head to
the word's `AscentY()` and its foot to `DescentY()`, so a caret is **exactly**
one line tall by construction and carries no other term. Ours is 24 device
rows against the oracle's 28 on the same 12-point field.

`password`'s remaining difference — a caret at column 189 against the
oracle's 199 — follows from the same place: with a shorter line the text is
laid out differently and the caret lands short of the field's right edge
where the oracle puts it against it.

The metric is `pdfrum-doc`'s (`ap::TextFont::metrics_of`, and beneath it
`pdfrum_font::Font::type_ascent`), so this is recorded rather than fixed here.
It is the single highest-value item left on the pixel side: it is the residue
of `scrollable_widgets{1,2}`, of `password`, and of what remains inside
`form_textfield_focused_ltr` after the tint was removed.

**Withdrawn 2026-09-01 (orchestrator, on the `pdfrum-doc` owner's
measurement, `scratchpad/m14-metric-drops.md`).** The metric premise above is
wrong and the residue is *this crate's*, not `pdfrum-doc`'s. With
`--croscore-font-names`, Helvetica → Arimo, whose face pair is **905 /
−211 = 1116** — the `hhea`/`win` pair (Arimo tables: hhea 905/−212, win
905/−212, typo 728/−210; no table yields ~1167) and the pair the oracle's
pixels agree with. `pdfrum-doc`'s list-box generator already emits a
13.392-high band at 12 pt (`1 2.2159996 98 13.392 re`, probed on
`scrollable_widgets2`). The 12-versus-14 rows come from
`crates/pdfrum-form/src/route.rs:741-746`: `row_height` returns the `/DA`
font size (12.0) where `CPWL_ListCtrl::Item::GetItemHeight`
(`cpwl_list_ctrl.cpp:49-51`) returns the laid-out
`GetContentRect().Height()` (13.392). The fix is to make `row_height` the
laid-out height — `vt::layout(label, &config, &metrics)
.content_rect_pdf(plate).height()`, the same call `ap::field_body::list_box`
makes per row — so `top_visible_for`, the wheel's visible-row count and the
hit test move together. No `pdfrum-doc` change; nothing was committed for
the metric. The caret and glyph-offset measurements above stand as
measurements; their attribution to the font tables does not.

### A divergence the port found, in `pdfrum-doc`'s selection reader

`/I [1 3]` on a field with no `/V` should select rows 1 and 3. It selects
**nothing**.

`CPDF_FormField::IsItemSelected` (`cpdf_formfield.cpp:546-554`) consults
**`/I` first**, as integer indices, and falls back to `/V` only when `/I` is
not *usable*. "Usable" is `UseSelectedIndicesObject` (`:863-935`): either
there is no `/V` at all, or `/I` has the same number of entries as `/V` and
every index names an option whose value appears in `/V`.

`ap::field_body::selected_indices` inverts both halves — it reads `/V` first,
consults `/I` only when `/V` is absent, and then compares each `/I` entry as
**text** against the option values, so an integer index matches no option.

The two readings agree on two of the three fixture fields, which is what let
the difference hide: with `/V` alone they agree trivially, and on
`Listbox_MultiSelectMultipleMismatch` (`/V` two entries against `/I` three)
upstream's consistency check *rejects* `/I` and falls back to `/V`, which is
the same answer. They differ on exactly one shape: **`/I` present, `/V`
absent.**

`crates/pdfrum/tests/form_listbox.rs::an_index_array_alone_currently_selects_nothing`
asserts the current answer with the rule in its doc comment, so correcting the
reader turns that test red rather than leaving it silently green. **The reader
is `pdfrum-doc`'s**, so this is recorded for that slice rather than fixed
here.

### What the port found that the corpus could not

Four defects, none visible in any `.evt` fixture, all caught by assertions
rather than by pixels. Worth stating together because they are the argument
for porting against the facade rather than against the edit control:

1. **Every click placed the caret at the same position.** The router passed a
   page-space point to a layout query that wants the appearance stream's
   space, so the hit test clamped to one end every time.
2. **A drag's release leg was dropped.** `mouse_up` cleared the anchor before
   extending the selection to the release point — and a drag whose only move
   *is* the release is exactly what upstream's `SelectTextWithMouse` sends.
3. **Tab could not reach the focus ring.** Keyboard dispatch refused every
   event without a focused field, and a Tab with nothing focused is what takes
   focus.
4. **A single-line field's text outran its layout.** The layout drops line
   breaks and the control stored the caller's string verbatim, so `"Foo\nBar"`
   reported seven characters against a layout holding six.
5. **An editable combo box reported no text.** `ChoiceState::focused_text`
   always answered with an option's label, and an editable box's text half is
   not an option.
6. **A focused link's Return did nothing.** `UpdateKind::ActionRequested` was
   declared and never constructed, so the one variant that exists to hand a
   caller a decision was unreachable.

The corpus renders none of these: its scripts click at a field's start, type
without clicking mid-text, and reach fields by clicking rather than tabbing.
