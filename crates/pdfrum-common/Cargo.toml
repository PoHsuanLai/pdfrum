[package]
name = "pdfrum-common"
description = "Shared error/diagnostic types and geometry vocabulary for pdfrum"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[lints]
workspace = true

# SPEC.md §1: this crate owns only Diagnostics/Limits and the kurbo re-export.
# It has no fallible operation, hence no `Error` enum and no `thiserror`.
[dependencies]
kurbo.workspace = true
