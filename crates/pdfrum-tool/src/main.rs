//! CLI mirroring the C++ oracle's `pdfium_test` flags and output formats
//! (`--png --md5 --txt --annot --show-metadata --show-pageinfo
//! --show-structure --save-images`, same filename conventions) so the
//! conformance harness diffs pdfrum against the oracle like-for-like
//! (SPEC.md §13).

#![forbid(unsafe_code)]

fn main() {
    eprintln!("pdfrum-tool: M0 stub - no functionality implemented yet");
}
